# Design Review: Phase 1 — Jobs, journey & IA

## Rendered Evidence (Step 0)
- Screenshot: none — spec-only phase, no rendered surface exists. Structure-level critique only; no pixel-level contrast/spacing/hierarchy claims are made or possible.
- Surface reviewed:
  - `/home/bewinxed/cockpit/JOURNEY.md` (81 lines, read in full)
  - `/home/bewinxed/cockpit/CLAUDE.md` (read in full)
  - Ground truth: `/home/bewinxed/cockpit/apps/dashboard/src/routes/` (route tree enumerated on disk)
- Doctrine read before reviewing: `references/journey/journey.md`, `references/journey/references/journey-stack.md`, `references/journey/references/journey-caveats.md`

## Assessment B — Deterministic Detector
- Command: not run.
- Exit: **3 — N/A (no rendered artifact)**. There is no `.html` or mock in this phase for `detect.mjs` to parse. This is the documented no-artifact carve-out, **not a skipped detector**, and does not affect the verdict.
- Findings: N/A — no rendered artifact.
- Opened only after Assessment A findings were frozen: N/A (nothing to open).

## Triage
- Baseline (always-on): visual + usability — applied at the only altitude available (structural/IA operability; no pixels to audit).
- Dispatched: `journey` — the artifact is a JTBD job story + NN/g journey map + IA/sitemap; the entire surface is journey doctrine.
- Not applicable: `data-viz` (no charts), `content-design` (no product copy — nav labels are reviewed under IA labeling, not content design), `behavioral` / `deceptive-patterns` (no persuasion surface; marketing spine is N/A), `design-dna` visual token review (no tokens or pixels exist yet — DESIGN.md is a later phase).
- Deferred: none.

## Cross-Pillar Findings (ONE ranked report)

| Severity | Pillar | Problem (in the artifact) | Principle | Fix |
|----------|--------|---------------------------|-----------|-----|
| **Critical** | journey | Line 18 reads `**Switch interview (Moesta four forces, documented via Christensen, *Competing Against Luck*, 2016):**`. The document declares `**JTBD school used:** Moesta (Switch interview)` (line 24) but imports the Christensen school's originator name and canonical text into the very heading of the four-forces block. Grep over the whole file for the other three schools' exclusive vocabulary returns exactly two hits, both on line 18: `Christensen`, `Competing Against Luck`. | JTBD school-mixing (`journey-caveats.md` §JTBD school-mixing; `journey.md` Rules: "Pick one JTBD school per project… Mixing produces neither"). The caveats table assigns *Competing Against Luck* / "hire for a job" to the **Progress/circumstance (Christensen)** school and push/pull/anxiety/habit to the **Switch interview (Moesta)** school as *incompatible* vocabularies. | Delete the `documented via Christensen, *Competing Against Luck*, 2016` clause and cite the four forces to their own school with its own year: `Switch interview (Moesta & Spiek, four forces, Rewired Group; Moesta *Demand-Side Sales 101*, 2020)`. This also fixes the wrong-claim citation below. |
| **Critical** | journey | `## Journey` is an NN/g journey map — actor/scenario/scope header plus the phases → actions → mindsets → emotion → touchpoints → opportunities swim lanes — and it carries **no citation to NN/g at all**. `grep -niE "nn/g\|nielsen\|gibbons\|swim.?lane\|journey map"` over JOURNEY.md returns **zero hits**. The largest structural claim in the document (that this table *is* the correct shape for a journey map) is uncited. Two further uncited structural claims in `## Job`: the job-story form `When…I want…so I can…` and the functional/emotional/social decomposition are presented with no author or year. | `journey.md` Rules: "**Cite the principle.** Every recommendation names its source: 'NN/g Gibbons 2018'… No unsourced structural claims." | Add `**Journey map structure:** NN/g swim lanes (Gibbons, "Journey Mapping 101", NN/g 2018).` to the `## Journey` header block, and attach the job-story form to its source (Klement job stories, jtbd.info 2013 — or the doctrine template) in `## Job`. |
| **Critical** | journey | Citation attached to the wrong claim. The only year anywhere in `## Job` is `2016`, and it is bound to Christensen's book, not to the Moesta four-forces framework the section is actually applying. Moesta — the declared school — carries no year anywhere in the document. | The prompt's explicit defect pattern ("a citation that is present but attached to the wrong claim"); `journey.md` Rules ("no unsourced structural claims"). | Same fix as row 1 — give Moesta its own author+year and remove the borrowed one. |
| **Major** | journey / usability | The `**Structure type:** Hub-and-spoke` claim (line 57) is justified by quoting the doctrine's fit condition *"no cross-navigation needed"* — and is contradicted by the document's own journey table two sections earlier. The **Steer or spawn work** phase (line 40) has the operator moving across `project/[id]`, `rules`, `rules/[id]` and `tools` inside a single phase, and its Opportunities cell explicitly asks to "Surface which rules and tools apply to a session at spawn time" — a contextual link from a session into two other spokes. That is cross-navigation, which the stated structure type says is not needed. | Rosenfeld/Morville/Arango 2015, structure-type fit table (`journey-stack.md` §Structure types); `journey-caveats.md` §Sitemap ≠ IA (a neat diagram can encode the wrong model). | Either drop the "no cross-navigation needed" justification and state hub-and-spoke *with* a declared contextual-navigation layer between Session detail ↔ Rules/Tools/Project, or reclassify the structure. The sitemap should show that edge rather than implying spokes are isolated. |
| **Major** | journey / usability | `**Global navigation labels:**` (line 70) removes the fleet board from persistent global nav ("not a persistent top-level nav item since it's the hub itself"), leaving Tools · Rules · Usage. But three of the six journey phases (Fleet check-in, Triage blocked sessions, Wind-down) terminate on the fleet board — it is by the document's own map the highest-frequency destination in the product, and it is the one destination with no specified persistent return affordance. The spec never says a wordmark or home control covers it. | Rosenfeld/Morville 2015 navigation system: global nav holds the top-level IA; `journey-caveats.md` §Sitemap ≠ IA ("Hiding frequently accessed content behind rarely-visited parent categories"). | Either keep the fleet board as an explicit global-nav item, or state the return affordance (wordmark → hub) in the navigation model so it is a specified decision rather than an omission. |
| **Major** | journey (labeling) | Nav labels are route slugs, not operator language. The document calls the surface "the fleet board" in prose eight times, then labels it `Session` in the nav — and labels the spend surface `Usage` while the journey phase is "Cost check" and the operator's question is "Am I about to blow the budget". The IA's own labeling system is never reconciled with the mental model the Job section establishes. | Rosenfeld/Morville 2015, Labeling system ("the language of the structure"); `journey-caveats.md` §Sitemap ≠ IA ("Choosing labels that are internal jargon rather than user-language"). | State route paths and display labels separately: route `session` → label **Fleet**; route `usage` → label **Spend** or **Cost**. Labels should come from the Job section's vocabulary, not the router's. |
| **Minor** | journey | Mis-cited section anchor: line 49 attributes the pre-flight check to `journey-caveats.md` §Journey-map theater. The pre-flight check is a separate section, §"Summary: the honest pre-flight check". The theater section covers grounding the emotion curve but is not the pre-flight check. | `journey.md` Rules: cite the principle accurately. | Point the reference at §Summary: the honest pre-flight check (or cite both). |
| **Minor** | journey | The map is flagged as a hypothesis with a research basis and a review trigger, but `**Owner:** not yet named` (line 49) — in a product the same document establishes has exactly one operator who is the only candidate. Declining to name the sole candidate leaves one pre-flight answer at "no" for no reason. | `journey-caveats.md` §Journey-map theater ("No one is named as the map's owner" is a theater indicator; "Name the map's owner before the workshop ends"). | Name the operator as owner. The single-operator constraint is a reason the owner is *obvious*, not a reason it is unnamable. |
| **Minor** | journey | The NN/g swim lanes list **Channels** as a lane distinct from Touchpoints; the table folds channel into the Touchpoints cell (e.g. "Telegram bridge, mobile web dashboard"). Desktop web / mobile web / Telegram are different channels with materially different constraints, and the Remote-approval phase's opportunity turns entirely on that difference. | NN/g swim-lane structure (`journey-stack.md` §Swim lane structure). Not a DW-1.4 violation — DW-1.4 enumerates six columns and six are present. | Optional: split channel out, or state explicitly that channel is carried inside Touchpoints for this map. |
| **Note** | journey | `## Flows` and `## Page specs` from the doctrine template are absent with no marker, while `## Marketing spine` is explicitly N/A'd. No DW item requires them at this phase, so this is not a defect — but the inconsistent handling (one template section explicitly dispositioned, two silently absent) is worth a one-line forward pointer. | `journey-stack.md` §JOURNEY.md template. | Add "Flows and Page specs: deferred to Phase 2" so absence reads as sequencing, not omission. |
| **Note** | process | `CLAUDE.md` is present on disk with the required block but is **untracked in git** (`git status --porcelain CLAUDE.md` → `??`; `git ls-files --stage CLAUDE.md` → empty). DW-1.8 asks that it exist at repo root, and it does — but a `git clean -fd` would remove it and the prior tracked copy is staged-deleted. | Not a doctrine principle; operational durability. | `git add CLAUDE.md` before the phase commit. |

## Requirement Fulfillment

### DW-1.1
PREMISE:  "`JOURNEY.md` exists at repo root with `## Job`, `## Journey`, `## IA` each complete."
EVIDENCE: File present at `/home/bewinxed/cockpit/JOURNEY.md`. `grep -n "^#"` returns exactly: `1:# JOURNEY.md`, `8:## Job`, `28:## Journey`, `53:## IA`, `78:## Marketing spine`. All three required sections present and populated — Job carries job story + functional/emotional/social + four forces + school declaration; Journey carries actor + scenario + scope + a 6-row table + decision model + emotion curve + research basis + owner; IA carries organization scheme + structure type + sitemap + global nav labels + navigation model + validation. No section is a stub or placeholder.
VERDICT:  **PASS**

### DW-1.2
PREMISE:  "`**JTBD school used:**` names exactly one school, and no vocabulary from the other three appears in the document."
EVIDENCE: Line 24 names exactly one school: `**JTBD school used:** Moesta (Switch interview).` — first half satisfied. Second half fails. I derived the four schools' exclusive vocabulary from `journey-caveats.md` §JTBD school-mixing (Christensen: "hire for a job"/"circumstance"/milkshake/*Competing Against Luck*; Ulwick: "desired outcome"/"Universal Job Map"/ODI/process steps; Moesta: push/pull/anxiety/habit/Switch; Klement: job stories/jtbd.info) and grepped for all of it. Result — two hits, both on line 18: `Christensen` and `Competing Against Luck`. The Moesta four-forces heading is attributed to the Christensen school's canonical text. (The functional/emotional/social triad and the `When…I want…so I can…` form are also cross-school in the caveats table, but both are mandated by DW-1.3 and by the doctrine's own JOURNEY.md template, so they are not counted against this item.)
VERDICT:  **FAIL**

### DW-1.3
PREMISE:  "Job story matches the literal `When [situation], I want [motivation], so I can [outcome]` form; Functional, Emotional, and Social jobs all populated."
EVIDENCE: Line 10 — "**When** several AI coding agents are running unattended across different machines and one of them stalls on a permission gate, breaks, or goes silent, **I want** to see which session needs me without opening every terminal or SSHing into every box, **so I can** approve, redirect, or kill it before it wastes the run's context and cost, or blocks work I'm waiting on." Literal three-clause form, in order. Line 12 `**Functional job:**` populated (triage / approve / spawn / budget). Line 14 `**Emotional job:**` populated (feel in control, not anxious). Line 16 `**Social job:**` populated (seen as running a disciplined, supervised fleet). All three are substantive and Cockpit-specific, not the doctrine's generic examples.
VERDICT:  **PASS**

### DW-1.4
PREMISE:  "Journey table carries all six columns (Phase · Actions · Mindset · Emotion · Touchpoints · Opportunities); header states actor, scenario, and scope; phase names are Cockpit-specific rather than the generic five."
EVIDENCE: Line 36 header row is exactly `| Phase | Actions | Mindset | Emotion | Touchpoints | Opportunities |` — six columns, all six populated in every one of the six rows. Header block above the table: line 30 `**Actor:**` (the fleet operator, single-tenant), line 32 `**Scenario:**` (a working day across in-person and mobile time), line 34 `**Scope:**` (future-state; touchpoints = browser dashboard + Telegram bridge; excludes daemon internals and harness CLI). Phase names: Fleet check-in · Triage blocked sessions · Steer or spawn work · Remote approval (away from the desk) · Cost check · Wind-down. None of these is the doctrine's template five (`Awareness → Consideration → Decision → Onboarding → Ongoing`); each is specific to this scenario.
VERDICT:  **PASS**

### DW-1.5
PREMISE:  "`**Research basis:**` reads a named source or the literal `UNGROUNDED`; `**Decision model:**` names loyalty loop or messy middle and never AIDA."
EVIDENCE: Line 49 opens `**Research basis:** UNGROUNDED.` — the literal token. Line 45 opens `**Decision model:** McKinsey loyalty loop (Court et al., *McKinsey Quarterly*, 2009; Edelman 2013)` and explicitly reasons why messy middle (Google, 2020) does not apply. `grep -niE "aida"` over the file returns zero hits; the only match for "funnel" is line 80, "Cockpit has no acquisition **funnel**" — a negation inside the Marketing-spine N/A, not a decision model. Citation spot-check: Court et al., "The consumer decision journey," McKinsey Quarterly 2009 is correct and matches the doctrine's own attribution; "Edelman 2013" is reproduced verbatim from `journey-stack.md` §McKinsey CDJ, so it is not an invention by this document (Edelman's loyalty-loop HBR pieces are 2010 and 2015 — flagged upstream, not charged here).
VERDICT:  **PASS**

### DW-1.6
PREMISE:  "IA states an organization scheme and structure type from the doctrine's enumerated sets; sitemap covers all 7 route surfaces; `**Validation:**` reads the literal `NOT VALIDATED`."
EVIDENCE: Line 55 `**Organization scheme:** Task` — in the doctrine's ambiguous set (topic / **task** / audience / metaphor). Line 57 `**Structure type:** Hub-and-spoke` — in the doctrine's enumerated structures (tree / sequential / matrix-faceted / **hub-and-spoke**). Sitemap (lines 60–68) lists 7 surfaces: Session, Session/[id], Project/[id], Tools, Rules, Rules/[id], Usage. Verified against disk — `find …/src/routes -name "+page.svelte"` returns `/+page.svelte`, `project/[id]`, `rules`, `rules/[id]`, `session/[[id]]`, `tools`, `usage`. The mapping holds: `session/[[id]]` is an optional-param route serving both the fleet board and session detail, which the sitemap correctly splits into the two distinct surfaces; `routes/+page.svelte` is a non-surface (its own comment reads "Never rendered: +page.server.ts redirects to the session index"); `api/[...path]` is not a UI surface. No sitemap entry is invented and no real surface is missing. Line 74 opens `**Validation:** NOT VALIDATED.` — the literal token. (The correctness of the hub-and-spoke *fit* is challenged as a Major finding above; this item asks that the type be named from the enumerated set, which it is.)
VERDICT:  **PASS**

### DW-1.7
PREMISE:  "Every structural claim carries an author/framework + year citation."
EVIDENCE: Many claims are cited correctly and check out: Fitts's law 1954 (line 39) ✓; Hick–Hyman 1952 (line 72) ✓; peak-end rule Kahneman 1993 (lines 43, 47) ✓ — Kahneman/Fredrickson/Schreiber/Redelmeier 1993 is the right work and year; McKinsey Court et al. 2009 (line 45) ✓; Rosenfeld/Morville/Arango, *Information Architecture for the World Wide Web* 4th ed, 2015 (lines 55, 57, 74) ✓ — the 4th edition is indeed 2015. But *every* is not met. `grep -niE "nn/g|nielsen|gibbons|swim.?lane|journey map"` returns **zero hits**: the entire `## Journey` section applies NN/g's journey-map structure (actor/scenario/scope header + six swim lanes) with no author and no year. In `## Job`, the job-story form and the functional/emotional/social decomposition carry no citation at all, and the Switch-interview four forces — the section's governing framework — carry no Moesta year; the only year present (`2016`) belongs to a different school's book, i.e. a citation attached to the wrong claim, which the review brief names as a defect in its own right.
VERDICT:  **FAIL**

### DW-1.8
PREMISE:  "CLAUDE.md **exists at repo root** and its `## Design Context` block contains `- **Journey spec**: JOURNEY.md`."
EVIDENCE: `ls -la /home/bewinxed/cockpit/CLAUDE.md` → present, 1171 bytes. File contains a `## Design Context` heading whose first bullet is verbatim `- **Journey spec**: JOURNEY.md`. Requirement is existence at repo root, which is satisfied on disk. (Untracked in git — recorded as a non-blocking Note, not charged against this item.)
VERDICT:  **PASS**

**All requirements met:** NO — DW-1.2 and DW-1.7 fail.

## Edge Case Handling

| Listed edge case | Evidence | Verdict |
|---|---|---|
| No card sort / tree test possible → sitemap must carry literal `NOT VALIDATED`, never invented validation | Line 74: `**Validation:** NOT VALIDATED.` followed by the reason (single-operator, no second person to test against) and a revisit trigger. No invented study anywhere in the file. | **HANDLED** |
| No user research → `**Research basis:**` must read literal `UNGROUNDED` and the map flagged as hypothesis per the pre-flight check | Line 49: `**Research basis:** UNGROUNDED.` plus "this map's emotion curve is a reasoned hypothesis from the stated forces and scenario, not derived from research". Explicit hypothesis flag. (The pre-flight anchor is mis-pointed at §Journey-map theater rather than §Summary: the honest pre-flight check — Minor finding above; the substance of the flag is correct. Owner left unnamed — Minor.) | **HANDLED** |
| `## Marketing spine` N/A → marked N/A explicitly, not 13 sections of invented content | Line 78–80: section present, opens `N/A.`, gives the reason (no acquisition funnel; installed by its own operator), names what is being omitted (persuasion spine, awareness-stage matching, StoryBrand) and states they "are omitted rather than filled with invented content". Zero of the 13 sections invented. | **HANDLED** |
| Journey phases must be scenario-specific; the doctrine's template five are a theater indicator | Six phases, all Cockpit-specific (Fleet check-in → Wind-down); no trace of Awareness/Consideration/Decision/Onboarding/Advocacy. | **HANDLED** |

All four listed edge cases are handled. No edge-case failure contributes to the verdict.

## Notes (non-blocking)
- **Pixel evidence gap:** no rendered surface exists at this phase, so contrast, spacing, hierarchy and type are entirely unverified. Nothing in this review speaks to them. This is expected for a spec-only phase and is not a defect.
- **Distinctiveness (ai-tells CHECKER mode, applied at spec altitude):** the document is not generic. It makes at least three choices a default generator would not: it argues *against* the messy middle rather than listing both models; it declares an explicit anxiety peak at mobile approval and derives a first-class requirement from it ("the mobile/compact surface must carry enough of the transcript's context to approve responsibly, not just a bare command string"); and it treats permission-widening as a Fitts's-law *hazard* (near-zero travel to a high-cost target) rather than a convenience. The phase names read like this product, not like a template. No distinctiveness failure.
- The two failing items are both citation-hygiene defects in an otherwise substantively strong document. Neither requires rethinking the job, the map, or the IA — row 1 is one clause to delete and replace, row 2 is one line to add. The Major IA findings (hub-and-spoke justification contradicted by the journey table; fleet board absent from global nav; route slugs used as labels) are the findings most worth acting on for the design itself, but none of them fails a listed requirement and none blocks this gate.

## Issues (blockers)
1. **Christensen-school vocabulary in a Moesta-declared document** (`JOURNEY.md:18`) — Critical / journey / JTBD school-mixing (`journey-caveats.md` §JTBD school-mixing) / Remove `documented via Christensen, *Competing Against Luck*, 2016`; cite the four forces to Moesta & Spiek with their own year. Fails DW-1.2.
2. **The NN/g journey-map structure is uncited** (`JOURNEY.md:28–49`, zero grep hits for NN/g / Gibbons / Nielsen) — Critical / journey / `journey.md` Rules, "no unsourced structural claims" / Add `**Journey map structure:** NN/g swim lanes (Gibbons, NN/g 2018)` to the Journey header. Fails DW-1.7.
3. **Year attached to the wrong claim** (`JOURNEY.md:18`) — Critical / journey / the brief's named defect pattern / Moesta carries no year; the only year in `## Job` belongs to another school's book. Fixed by issue 1. Fails DW-1.7.

**Verdict: FAIL — blockers: DW-1.2 (Christensen vocabulary on line 18 in a Moesta-declared document); DW-1.7 (NN/g journey-map structure uncited; Moesta four forces carry another school's year and none of their own).**
