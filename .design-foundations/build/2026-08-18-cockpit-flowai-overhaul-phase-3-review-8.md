# Design Review: Phase 3 — Cockpit / FlowAI overhaul (review 8)

## Rendered Evidence (Step 0)
- Screenshots read as pixels: `/home/bewinxed/cockpit/mocks/v2-fleet.png`, `v2-dark.png`, `v2-mobile.png`, `v3-assistant.png`, `v4-transcript.png`
- Additional renders captured by me for probing: forced-colors @1440, `prefers-contrast: more` @320 and @390, coarse-pointer target sweep at 320/360/768/1024/1280/1440 × both schemes
- Surface: three high-fidelity mocks (fleet board, assistant panel, session transcript) plus `DESIGN.md` (865 lines) as the contract
- Suite re-run independently: `bash /home/bewinxed/cockpit/mocks/verify.sh` → **121 PASS, 0 FAIL, exit 0**, and its own byte-identity gates held (`a fresh build reproduces the checked-in mocks byte for byte`; `DESIGN.md regenerates byte for byte from the current reports`)

## Assessment B — Deterministic Detector
- Command: `node /home/bewinxed/.claude/plugins/cache/rtd/design-for-ai/4.2.0/scripts/detect.mjs mocks/v2-fleet.html mocks/v3-assistant.html mocks/v4-transcript.html > /tmp/detect-p3h.json`
- Exit: **0** (`"status": "ran"`), stderr empty
- Findings: **18, all one rule — `nested-cards` (high)**. Zero hits on every other rule in the catalog (no `purple-triplet`, no generic-shadow, no default-radius, nothing else).
- Opened only after Assessment A findings were frozen: **YES**

## Triage
- Baseline (always-on): **visual** (design-dna, ai-tells CHECKER) + **usability**
- Dispatched: **content-design** (real product copy throughout — KPI labels, permission-scope copy, empty/assistant states); **journey** (the fleet → session → permission route is the product's spine); **behavioral + deceptive-patterns** (the surface contains a consent gate on a destructive shell command and a permission-widening control)
- Not applicable: **data-viz** — there is no chart, graph or numeric encoding anywhere in the three mocks; the KPI tiles are single scalars with a label, not an encoding
- Deferred: none

---

## Cross-Pillar Findings (ONE ranked report)

| Severity | Pillar | Problem (in the rendered pixels) | Principle | Fix |
|---|---|---|---|---|
| Major | behavioral / deceptive-patterns | On the `rm -rf` permission gate, size parity is verified but **salience parity is not**. Painted-pixel measurement on `v4-transcript.png`: Approve fill `rgb(48,50,53)` = **12.62:1** against the panel it sits on; Deny fill `rgb(239,240,243)` = **1.12:1** against the same panel. DESIGN.md's rationale is "nothing in this design should nudge an operator under time pressure toward granting a destructive command", and its evidence is peer *size and weight* (mean grey 67.2 vs 235.4) — which is the asymmetry restated as a feature. Two controls at 12.6:1 and 1.1:1 are not peers in the channel that actually decides a fast choice. | Visual salience / von Restorff; Nielsen #5 error prevention; deceptive-patterns "visual interference" | Phase 4: give Deny an equal-salience treatment (matched border weight + ink contrast) or drop Approve to a peer-salience recessed form on *destructive* grants specifically. Not a blocker because the scope copy is exemplary and honest — Machine / Path / Network / **Undo: regenerated by the next `vite build`** / **Future: does not widen what the agent may run later** — so the operator is informed, not misled. |
| Major | behavioral / usability | **The widest-scope control on the screen is the least differentiated one.** "Always allow `rm -rf` in ~/cockpit" paints at **1.00:1** against its panel — treatment-identical to the benign quick-reply chips ("Start time", "Session id") 700px above it — while its effect ("every session in this folder, on every machine") is strictly larger than the single command being approved. Its honest disclosure line sits *below* the control in muted ~11.5px ink. | Nielsen #5 error prevention; escalation friction should scale with scope | Phase 4: move the scope sentence above the control, and make the escalation visually heavier (or two-step) than the one-shot grant it sits under. |
| Major | journey / usability | **The board's stated job is not reachable from the default view.** The subtitle promises "Every agent across your machines, and what needs you." The KPI reads **Needs you 3**; the default sort is "Last active"; page 1 shows exactly **2** `needs you` chips (`v2-fleet.html` contains exactly two `chip-s s-attn` instances) against "Showing 8 of 24". The third is on another page. The KPI is a static `<div class="stat">` — not a link, button or filter — so the only route from "3 need you" to those three sessions is manually driving the "All states" dropdown. | Nielsen #1 visibility of system status; #7 flexibility and efficiency of use | Phase 4/6: make the Needs-you KPI a filter affordance, and/or float `needs you` rows to the top of the default sort. |
| Major | layout / journey (mobile) | **On the 390 board the first session sits ~750 CSS px down** (measured from `v2-mobile.png`, 780×1710 @2×: first session name at display y ≈ 1497 → CSS y ≈ **748**) — below the fold on any phone. Ranked above it: date picker, Start session, four KPI cards, search, three filter dropdowns, and **Export CSV** — a desktop-only affordance placed above the primary content, on the device the brief calls first-class ("the operator approves permissions from a phone under time pressure"). | Priority of content / progressive disclosure; Nielsen #8 aesthetic and minimalist design | Phase 4/6: collapse the filter cluster behind one control on coarse pointers, drop Export CSV below the list, and lead with the needs-you set. |
| Major | content-design | **Four different session counts on one screen.** Sidebar badge **6** · KPI "Sessions **6**" · "Running now" list **4** items · table "Showing 8 of **24**". "Sessions" carries no qualifier, so the KPI and the table directly beneath it disagree by 4×. Fixture data, but the *label* is the design artifact. | Nielsen #4 consistency and standards; #1 visibility of system status | Qualify the KPI ("Active sessions") and make the fixture counts reconcile, so the number an operator quotes matches the list they scroll. |
| Minor | detector | 18 × `nested-cards` (high) — evidence verbatim: `<div class="ghost"> is a card inside a card ancestor`, `<div class="icobtn"> …`, `<div class="search"> …`, `<button class="sel"> …`. **Every one of the 18 is a toolbar control inside a panel, not a card-in-card.** Container-level verification in the live DOM: the only genuine nesting is the signature well — `.stat` padding **7px** radius **10px** containing `.well` padding **13px 15px** radius **7px**; different inset, different fill, different radius. That is the form DESIGN.md `## Never` #1 explicitly permits and the DW-3.9 edge case explicitly carves out. | ai-tells `nested-cards`, resolved by register (dispatch prompt: "detect.mjs fires many false positives on nested-cards — container-level verification required") | No change. Recorded as a register-justified hit, not a defect. |
| Minor | process / coverage | **Two cells the suite's own crossing rule requires but does not run.** `axischeck.mjs` line 73 lists "target size x pointer x width" as a crossing it runs, but `coarse: !!axis.touch` is set on **2 of 5** widths (390, 1024) — 320 and 1440 are never evaluated under a coarse pointer, and DESIGN.md's conditions column reads "`pointer: coarse` at **every** width". Separately, `prefers-contrast` is classified STRUCTURAL in the same header ("change which rules apply") yet runs at **1440 only**, so it never multiplies with width as the stated rule demands. **I ran both cells independently and both hold:** coarse pointer at 320/360/768/1024/1280/1440 × both schemes → **0 targets under 43.5px**; `prefers-contrast: more` at 320 and 390 → `320/320` and `390/390` document width, no overflow, all chips intact. | The suite's own stated crossing rule; "a property verified where an element does not exist is not verified" | Add `touch: true` at 320 and 1440 and a `prefers-contrast @390` axis. Gate-coverage gap, **not** a render defect — but it is the exact shape of the last four Criticals. |
| Minor | visual / a11y | Under `forced-colors: active`, `needs you`, `working` and `error` all collapse to identical pills — measured `rgb(255,255,255)` background / `rgb(0,0,0)` ink on all three — leaving the SVG glyph as the *only* differentiator. It works (every chip carries a distinct glyph, verified present on all 8), but the forced-colors user has one cue where everyone else has three. | WCAG 1.4.1 use of colour; the design's own "hue is the third cue" rule | Phase 4: map status to `SystemColor` keywords (`Mark`, `Highlight`, `GrayText`) so a second channel survives. Predicted and handled by the design; noted for completeness. |
| Note | visual / distinctiveness | **Passes the CHECKER criterion.** Nameable in three words: *recessed-well ledger*. The choice a generic system would not make: the card **contains a hole** — a measured 267×76 r7 well inset inside a 281×90 r10 raised card — paired with an accent that is deliberately **graphite, not a hue** (`--brand-solid` = `--neutral-12`), a colour budget of 2.342% of surface, asymmetric 25/21 content padding, and a non-4px spacing ladder (4/7/11/14/18/21/25/32). This is not on-pattern default output. | ai-tells.md CHECKER mode | — |

---

## Requirement Fulfillment

### DW-3.1
PREMISE: *(smoke check)* `palette.mjs` exits 0 with no FAIL lines. NOTE: the DW text says `--scheme both`, which exits 1 with `missing --seed`; score against the project's documented invocation and note the wording defect.
EVIDENCE: Bare `node palette.mjs --scheme both` → `exit=1`, stderr `palette.mjs: missing --seed (hue 0-360 or #hex)` — the wording defect is real and reproduced. The project's documented invocation `--seed 263 --chroma muted --harmony analogous --scheme both` → **exit=0, FAIL lines=0**, stderr 0 bytes. Its stdout is **byte-identical** to the checked-in `mocks/palette-263-muted-analogous.css` (`diff -q` → IDENTICAL), so the file the DW-3.2 gate reads is the generator's own output, not a drifted copy.
VERDICT: **PASS** (DW wording defect noted, not scored against the build)

### DW-3.2
PREMISE: `--neutral-1`…`--neutral-12` and `--accent-1`…`--accent-12` in both schemes; all 13 semantic aliases resolve; functional colors `--error/success/warning/info-3/9/11` defined.
EVIDENCE: Suite output — `light: 61 tokens … missing none`, `dark: 61 tokens … missing none`, covering 12+12 ramp, 12 functional and 13 aliases in both blocks. Independently confirmed against `tokens.resolved.css`: every one of the 33 names the contrast gate dereferences resolves to a concrete hex in both `:root` and `.dark`.
VERDICT: **PASS**

### DW-3.3
PREMISE: Pairs `palette.mjs` does not verify, measured independently in both schemes: `--error-11`, `--warning-11`, `--success-11`, `--info-11` on `--surface` AND `--surface-hover` ≥ 4.5:1 · `--text-secondary` on `--surface-hover` and `--surface-active` ≥ 4.5:1 · `--accent-solid` on `--background` ≥ 3:1 non-text. Every status pill measured from painted pixels. NOTE: verify the collapsed-values claim independently.
EVIDENCE: I re-computed all 22 pairs myself with my own WCAG implementation from `tokens.resolved.css`, not from their report. Light — error-11 `#86534f` on surface **5.92**, on surface-hover **5.48**; warning-11 `#6f6144` **5.74 / 5.31**; success-11 `#486e49` **5.53 / 5.12**; info-11 `#4c677a` **5.65 / 5.22**; text-secondary on surface-hover **5.28**, on surface-active **4.90**; accent-solid on background **5.51**. Dark — **8.54 / 7.74**, **8.79 / 7.97**, **9.00 / 8.16**, **8.86 / 8.03**; text-secondary **7.93 / 7.15**; accent-solid on background **3.32**. All ≥4.5 (≥3 for the non-text pair). **Collapsed-values claim verified independently, not accepted:** `resolve-tokens.mjs` now assigns each token to a probe element's `color` against an `rgb(1,2,3)` sentinel on the *parent*, so a non-colour token is recorded as skipped rather than as `#000000`; every one of the 33 dereferenced names is a real colour token and none reads black. **Painted-pixel check, done by me on the PNGs rather than trusting `paintcheck.mjs`:** light chips — needs-you fill `(240,232,214)` **7.43:1**, working `(216,232,244)` **7.17:1**, error `(248,217,214)` **7.01:1**, idle `(252,253,253)` **5.90:1**; dark — **7.89 / 8.17 / 8.59 / 7.93**.
VERDICT: **PASS**

### DW-3.4
PREMISE: The dark ramp activates under the project's `.dark` class variant — verified by rendering.
EVIDENCE: `measure.mjs` renders and reports light `body-bg #f3f4f6 / ink #2c2e31 / --neutral-1 #fcfdfd` → dark `#19191a / #e6e8ec / #121313`; ground 244→25, ink 46→233, and `--neutral-1` **re-solves** rather than inverting. Confirmed on my own renders: adding `.dark` in a fresh browser context flips every chip fill and ink (`(240,232,214)` → `(58,52,38)` etc.). `build-tokens.mjs` rewrites the generator's `[data-theme="dark"] {` to `[data-theme="dark"], .dark {`.
VERDICT: **PASS**

### DW-3.5
PREMISE: Type scale `--text-xs`…`--text-4xl` with `--font-body` and `--font-display`; `## Type` states ratio, base px, steps, leading, weights — and those statements must match what renders.
EVIDENCE: `tokens.css` ships all nine steps in **rem** — `0.640625 / 0.71875 / 0.8125 / 0.90625 / 1.03125 / 1.15625 / 1.296875 / 1.46875 / 1.640625` — plus `--font-body` (Geist Variable), `--font-display` (= body), `--font-mono`. Base `0.8125rem` × 16 = **13px**; ratio **1.125** rounded to 0.25px reproduces every rung. `## Type`'s claims are gated on the render, not asserted: computed sizes across all three mocks are `{"13px":2264,"14.5px":376,"11.5px":990,"23.5px":44,"20.75px":48,"18.5px":8,"16.5px":10,"10.25px":10}` — every one a named step; computed leading is `1.400×6300 · 1.250×900 · 1.000×280`, all inside the stated 1.2–1.4 band; computed weights are `{"400":2338,"450":380,"500":1032}` — **no 600+**.
VERDICT: **PASS**

### DW-3.6
PREMISE: Primary face is none of Inter / Roboto / Open Sans / Arial / Space Grotesk, installs from `@fontsource-variable/*`, with a justification tying letterform to the comps. NOTE: verify the dependency claim; judge "installs" given `apps/dashboard` still renders `public-sans`.
EVIDENCE: `apps/dashboard/package.json` lines 15–16 declare `"@fontsource-variable/geist": "^5.3.0"` and `"@fontsource-variable/geist-mono": "^5.3.0"`; `bun.lock` carries resolved entries with integrity hashes (`@fontsource-variable/geist@5.3.0`, `sha512-j0m+vLQ…`) — a real, resolvable dependency, not a vendored file. Registry reachable (HTTP 200). None of the banned five appears in `tokens.css`. Justification is measured from the shipped file, not asserted: x-height **0.530 em** against cap **0.710 em** at unitsPerEm 1000, continuous `wght` 100–900, yielding a **6.9px x-height** at the 13px base — the stated reason the 400/450/500 ladder is possible at all. "Installs" is met on its own terms; `apps/dashboard` still listing `public-sans` at line 29 is the app-wiring step, which DESIGN.md's `## Open questions` declares openly ("DW-3.6 is met as *installable*, not as *shipping*") and the plan assigns to Phase 4.
VERDICT: **PASS**

### DW-3.7
PREMISE: Accent contains none of `#6366F1`, `#8B5CF6`, `#A855F7`; palette is neither cyan-on-dark nor purple-to-blue.
EVIDENCE: `--accent-9` / `--accent-solid` = **`#4466ac`** in both schemes — a mid-dark slate blue. Zero occurrences of any banned literal across `tokens.css` and all three mocks. CIEDE2000 distance from the triplet: **11.2 / 17.4 / 22.4**. Not cyan-on-dark (the dark ground is `#121313` graphite, and the accent is used as a solid on light); not purple-to-blue (the only gradient in the system, `--gradient-action`, has both stops in graphite: `oklch(0.375347 0.00609613 258.468)` → `oklch(0.288347 …)`).
VERDICT: **PASS**

### DW-3.8
PREMISE: DESIGN.md carries all 10 template sections, `**Status:** confirmed`, and a `**Pins:**` line.
EVIDENCE: Section headings read from the file: Direction · Signature move · Expressive moments · Type · Color tokens · Interaction states · Space, shape, depth · Motion · Never · Open questions — 9 template sections plus the header block = 10. Line 2 carries `**Status:** confirmed`; line 8 carries `**Pins:** family=data-dense-professional · discipline=ledger-grid · hue=263 · chroma=muted · signature=accent-scarcity`. The gate checks by *name*, not by count, so an added section cannot mask a missing one.
VERDICT: **PASS**

### DW-3.9
PREMISE: `## Never` names the uniform-padding form of `nested-cards` plus ≥2 further tells scoped to this DNA, and explicitly permits the comps' inset-well surface.
EVIDENCE: `## Never` #1 names "**Nested cards in the uniform-padding form**" and immediately states "**Explicitly permitted, and required:** the comps' **inset-well** surface — a raised card containing a *recessed* panel at a different inset (7px), a different fill and a different radius (7 inside 10)". Nine further tells follow, all scoped to this DNA (decorative hue; red as ambient colour; unmodified shadcn defaults; weight above 500; pure-black shadow; uniform spacing; a scroll container standing in for readable content; content trapped behind a non-scrolling ancestor; matching a comp defect). The permitted form is confirmed in the live DOM — `.stat` 7px/r10 containing `.well` 13px 15px/r7.
VERDICT: **PASS**

### DW-3.10
PREMISE: Shipped `--radius`, spacing, and shadow values differ from shadcn's defaults; no shadow uses pure black.
EVIDENCE: Radius ladder read from `tokens.css`: **10 base · 4.6 mark · 5.5 tile · 7 well · 8 control · 10 card · 14 panel · 999 pill** — against shadcn's 4/6/8/12 off `--radius: 0.5rem`; differs in five of six members and in cardinality. Spacing: **4 · 7 · 11 · 14 · 18 · 21 · 25 · 32** — not a 4px-multiple ladder. Pure-black colour values in `tokens.css` with comments stripped: **0**; pure-black values inside any mock `box-shadow`: **0**. Every elevation token tints from `--neutral-12` via `oklch(from …)`.
VERDICT: **PASS**

### DW-3.12
PREMISE: Status chips use only the four functional hues; any non-status accent is visibly distinct from all four. Status stays legible with hue removed. Saturated colour only on chips, badges, and small marks.
EVIDENCE: DOM measurement: 8 chips, 4 states, **every chip carries a glyph as well as a label**, every state a distinct glyph; idle ships `rgba(0,0,0,0)` — no fill at all. The non-status accent is `--brand-solid` = `--neutral-12` (graphite), at CIEDE2000 **39.7 / 57.9 / 61.6 / 47.5** from the four functional hues in light and **36.3 / 23.7 / 27.7 / 22.3** in dark. Hue-removed legibility confirmed on my own forced-colors render: all tints strip to white/black, and the five states remain separable by glyph plus filled-vs-unfilled. Colour budget measured on the board: **2.342%** of surface carries any hue, largest hued region **0.1202%**.
VERDICT: **PASS**

### DW-3.13
PREMISE: `mocks/fidelity.py` exits 0. Proxies are not box geometry — `stat_run_w` reads 264 where the box is 280, `stat_run_gap` reads 30 where the CSS gap is 14. Never reconcile these against the measured-reference table.
EVIDENCE: Re-ran the suite; `fidelity.py` exits 0 and prints `PASS`. Every quantity within tolerance: `card_left 253/253 +0`, `card_right 1418/1418 +0`, `cell_pad_l 23/23 +0`, `row_pitch 44/44 +0`, `content_pad_r 21/21 +0`, `sidebar_w 228/227 −1`. The painted-run proxies read exactly as the DW predicts — **`stat_run_w` 264→265** and **`stat_run_gap` 30→30** — and I did **not** reconcile them against the 280×90 / 14px reference table; they are painted white-run measurements across a card containing a recessed well, which is a different quantity.
VERDICT: **PASS**

### DW-3.13b
PREMISE: AA beats fidelity where the comp is wrong — the reference's `#838383` on `#F1F1F1` is 3.36:1; such inks are judged on their own AA ratio and reported as accepted deviations.
EVIDENCE: `gate-report.txt` reports `band_label_ink  ref #838383  build #646464  −31L  ok  AA 5.24:1 on #F1F1F1 (ref 3.36:1 — below 4.5, override)`, and prints a dedicated block: `AA overrides — reference is defective here; build judged on AA, not on match: band_label_ink build 5.24:1 on #F1F1F1 ref 3.36:1 min 4.5 PASS`. `## Never` #10 records the same as policy. The deviation is declared, measured and reported — not silently absorbed.
VERDICT: **PASS**

### DW-3.13c
PREMISE: The three mocks carry zero raw hex and `fidelity.py` still exits 0.
EVIDENCE: `v2-fleet.html: 0 raw hex · v3-assistant.html: 0 · v4-transcript.html: 0`, and the notation-agnostic `literalcheck.py` reports `0 hand-typed colour literal(s)` per file with "every colour in every mock resolves through a token". `fidelity.py` exits 0 in the same run.
VERDICT: **PASS**

### DW-3.14
PREMISE: The primary action is not flat — top-highlight gradient — and the border scale stays graded (three distinct values).
EVIDENCE: Measured in the live DOM: `.cta background-image: linear-gradient(oklch(0.375347 0.00609613 258.468), oklch(0.288347 0.00609613 258.468))` — two distinct stops, top highlight to body — plus an inset bottom edge `oklch(0.255347 …) 0 -1px 0 0 inset`. Border scale computes to **three distinct values**: hairline `oklab(0.946851 …)` · divider `oklab(0.937497 …)` · control `rgb(230,232,236)`. Visible in `v4-transcript.png`: the Approve button carries the lit-from-above read.
VERDICT: **PASS**

**All requirements met:** YES (14 of 14 — DW-3.11 out of scope)

---

## Notes (non-blocking)
- The detector's 18 hits are one rule with one root cause and are register-justified; the only true nesting in the system is the signature well, whose inset, fill and radius all differ from its parent.
- I re-ran the whole suite rather than reading its published output: **121 PASS, 0 FAIL, exit 0**, and both self-consistency gates held — the mocks rebuild byte-for-byte from `mocks/src/`, and `DESIGN.md` regenerates byte-for-byte from the current reports. The "gate passing on a wrong input" probe came back clean: the file DW-3.2 reads is byte-identical to a fresh `palette.mjs` run I performed myself, and the DW-3.3 resolver reads real browser-computed colours behind a sentinel that makes non-colour tokens *skip* rather than record as black.
- The document's `## Open questions` already declares the honest residue — installable-not-shipping fonts, the 3.32:1 dark accent margin, absolute 44px row pitch, unenforced RTL/print/scrollbars. I found no undeclared claim outrunning its conditions; the closest candidate — "`pointer: coarse` at **every** width" — I tested at six widths × two schemes and it **holds**, so the gap is in the gate's coverage, not in the render.
- Nothing on any surface uses red as an ambient colour: `--error-*` appears only on the failed-session chip and destructive confirmation.

## Issues (if FAIL)
None meeting the blocking bar. Nothing on this surface makes content unreadable or unreachable, misleads an operator about a destructive action or a system state, or contradicts a DESIGN.md claim under the document's own stated conditions. The five Majors are page- and component-phase work (Phase 4/6) on a surface whose token and identity layer is sound.

**Verdict: PASS**
