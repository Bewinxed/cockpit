# Gates: A2 — DB schema + migration
Owned: packages/hub/src/db/schema.ts, packages/hub/src/db/index.ts, packages/hub/drizzle/*(new migration only). Forbidden: server.ts, rules.ts. POLICY: no new tests — verify via existing suite + throwaway scripts (not committed).

- [x] G1 rules table gains trigger/action/prompt cols (defaults 'pattern'/'reply'/null); instances gains autopilot JSON col; new supervisor_events + supervisor_config tables per C5/C6/C4
  CHECK: cd /home/bewinxed/cockpit && ls packages/hub/drizzle/ | tail -3
  EXPECT: one new migration file
  EVIDENCE: Migration `drizzle/0029_square_purple_man.sql` generated. Contains:
  - `ALTER TABLE rules ADD trigger text DEFAULT 'pattern' NOT NULL`
  - `ALTER TABLE rules ADD action text DEFAULT 'reply' NOT NULL`
  - `ALTER TABLE rules ADD prompt text`
  - `ALTER TABLE instances ADD autopilot text`
  - `CREATE TABLE supervisor_events` (id autoincr, instance_id, source, rule_id, verdict, message, note, model, latency_ms, created_at; index on instance_id+created_at)
  - `CREATE TABLE supervisor_config` (id text pk, enabled bool default false, base_url, model, api_key, updated_at)
- [x] G2 Migration applies to a scratch db and round-trips an llm rule, autopilot JSON, one supervisor event, config row — via a THROWAWAY bun script under /tmp (run, paste output, delete; do not commit)
  EVIDENCE: Throwaway /tmp/a2-verify.ts ran against /tmp/a2-scratch.db. Output:
  ```
  --- LLM rule write+read ---
  trigger: pattern | action: llm | prompt: Check whether the agent actually ran the tests and pasted output
  LLM rule OK: true
  every-turn OK: true
  --- autopilot ---
  autopilot JSON: {"enabled":true,"prompt":"Keep the session on track","updatedAt":1788254652963}
  autopilot OK: true
  --- supervisor config ---
  before: undefined
  after: true http://localhost:30000 qwen3.8 null
  config OK: true
  --- supervisor events ---
  event id: 1 | verdict: reply | latencyMs: 4200
  listed events: 1
  event OK: true
  === ALL CHECKS PASSED ===
  ```
  Script and scratch db deleted.
- [x] G3 DbShape gains recordSupervisorEvent/listSupervisorEvents + supervisor config get/put + autopilot write; prune-to-5000 on insert exercised by the same throwaway script
  EVIDENCE: From same script output:
  ```
  --- prune proof (5010 inserts) ---
  total events after 5010 inserts: 5000
  prune OK: true
  ```
- [x] G4 Existing hub suite unbroken + typecheck clean
  CHECK: cd /home/bewinxed/cockpit && bun test packages/hub
  EXPECT: failures not beyond A1 baseline
  EVIDENCE:
  - `npx tsc --noEmit`: clean (no output)
  - `bun test packages/hub`: 180 pass, 0 fail, 1396 expect() calls (matches A1 baseline exactly)
  NOTE: Fixed compile-only breakage in rules.test.ts (added trigger/action/prompt defaults to the test's draft() helper — no new assertions, permitted maintenance per brief).
