# Gates: A2 — DB schema + migration
Owned: packages/hub/src/db/schema.ts, packages/hub/src/db/index.ts, packages/hub/drizzle/*(new migration only). Forbidden: server.ts, rules.ts. POLICY: no new tests — verify via existing suite + throwaway scripts (not committed).

- [ ] G1 rules table gains trigger/action/prompt cols (defaults 'pattern'/'reply'/null); instances gains autopilot JSON col; new supervisor_events + supervisor_config tables per C5/C6/C4
  CHECK: cd /home/bewinxed/cockpit && ls packages/hub/drizzle/ | tail -3
  EXPECT: one new migration file
  EVIDENCE: pending
- [ ] G2 Migration applies to a scratch db and round-trips an llm rule, autopilot JSON, one supervisor event, config row — via a THROWAWAY bun script under $CLAUDE_JOB_DIR/tmp or /tmp (run, paste output, delete; do not commit)
  EVIDENCE: pending
- [ ] G3 DbShape gains recordSupervisorEvent/listSupervisorEvents + supervisor config get/put + autopilot write; prune-to-5000 on insert exercised by the same throwaway script
  EVIDENCE: pending
- [ ] G4 Existing hub suite unbroken + typecheck clean
  CHECK: cd /home/bewinxed/cockpit && bun test packages/hub
  EXPECT: failures not beyond A1 baseline
  EVIDENCE: pending
