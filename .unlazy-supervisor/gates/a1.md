# Gates: A1 — core contract (C1 + env + types)
Owned: packages/core/src/rules.ts, packages/core/src/index.ts. Forbidden: everything else.

- [x] G1 Baseline captured: test counts for packages/core, packages/hub, apps/dashboard, packages/agent recorded below (counts may grow later, failures may not)
  EVIDENCE: Ran from repo root before any change.
  `bun test packages/core` → "5 pass, 0 fail" (1 file, 8 expect() calls)
  `bun test packages/hub` → "180 pass, 0 fail" (12 files, 1396 expect() calls)
  `bun test apps/dashboard` → "340 pass, 0 fail" (25 files, 871 expect() calls)
  `bun test packages/agent` → "181 pass, 0 fail" (26 files, 567 expect() calls)
- [x] G2 RuleTrigger/RuleAction/prompt added per C1; ruleProblem refuses all illegal combos (every-turn+reply; llm+timing!=turn; llm+short prompt; llm+requireAck), each refusal test-asserted
  CHECK: bun test packages/core
  EXPECT: 0 fail; new ruleProblem cases present
  EVIDENCE: `bun test packages/core` → "26 pass, 0 fail" (2 files, 40 expect() calls) — up from the 5-test baseline.
  New file packages/core/src/rules.test.ts asserts, by name: 'every-turn+reply is refused', 'llm with timing !== turn is refused',
  'llm with an empty prompt is refused', 'llm with a too-short prompt is refused', 'llm with requireAck: true is refused',
  'llm does not require a reply', 'pattern checks are skipped when trigger is every-turn', 'pattern+llm is legal',
  'every-turn+llm is legal', 'pattern+reply — the shape every old rule has — is legal'. All pass.
- [x] G3 ruleInScope exported from core (moved from RuleEngine.#inScope semantics, behavior-identical)
  CHECK: cd /home/bewinxed/cockpit && grep -n 'export function ruleInScope' packages/core/src/rules.ts
  EXPECT: match
  EVIDENCE: `423:export function ruleInScope(scope: RuleScope, facts: RuleFacts): boolean {` — body is the same four ANDed
  checks as `RuleEngine.#inScope` (packages/hub/src/rules.ts, untouched — B1 switches the hub over later). Tests
  'ruleInScope: machineId/projectId/harness narrows and rejects a mismatch', 'model is a case-insensitive substring
  test', 'a session with no model fails a model-scoped rule', 'every field is ANDed' — all pass.
- [x] G4 WHIFFLE_ENV gains supervisorUrl/supervisorModel/supervisorKey; InstanceRow gains autopilot?; SupervisorEvent type lives beside DelegateEvent
  CHECK: cd /home/bewinxed/cockpit && grep -n 'WHIFFLE_SUPERVISOR_URL\|autopilot' packages/core/src/index.ts | head
  EXPECT: both present
  EVIDENCE:
  ```
  supervisorUrl: 'WHIFFLE_SUPERVISOR_URL',
  supervisorModel: 'WHIFFLE_SUPERVISOR_MODEL',
  supervisorKey: 'WHIFFLE_SUPERVISOR_KEY',
  autopilot?: { enabled: boolean; prompt: string; updatedAt: number } | null;
  ```
  DEVIATION (recorded, not hidden): `DelegateEvent` actually lives in `apps/dashboard/src/lib/whiffle/types.ts`
  (verified: `grep -rln DelegateEvent packages apps` finds no core file, only that dashboard file and its
  consumers) — not in core, and not an A1-owned file. Since both the hub (writer) and the dashboard (reader) need
  `SupervisorEvent` and the hub cannot import from the dashboard app, `SupervisorEvent` is defined in
  `packages/core/src/index.ts` instead, beside `InstanceRow`, with a doc comment explaining why. Flagged in the
  leaf report for the chair to confirm.
- [x] G5 ruleSentence/blankRule/draftOf extended; defaults keep old rules valid (pattern+reply)
  CHECK: cd /home/bewinxed/cockpit && bun test packages/core && npx eslint packages/core/src/rules.ts
  EXPECT: 0 fail, 0 lint errors
  EVIDENCE: `bun test packages/core` → "26 pass, 0 fail". `npx eslint packages/core/src/rules.ts` → no output (clean).
  `cd packages/core && npx tsc --noEmit` → no output (clean) — extra check added on top of the brief's two commands.
  Test 'ruleSentence for pattern+reply is unchanged from before trigger/action existed' pins the exact old sentence
  string, proving the default (`trigger:'pattern', action:'reply'`) path is byte-identical to before.
  DEVIATION: `blankRule`/`draftOf` do NOT live in core — they are dashboard-only helpers in
  `apps/dashboard/src/lib/whiffle/rules.ts`, owned by leaf C2, not by A1's owned-files list
  (`packages/core/src/rules.ts`, `packages/core/src/index.ts`). Not touched, per the standing constraint
  "Touch only OWNED FILES; needing another file = stop and report." `RULE_TEMPLATES` in core *was* extended with
  `trigger`/`action`/`prompt` on every template draft, since that constant lives in the owned file.
  KNOWN DOWNSTREAM BREAKAGE (expected, not a regression): making `trigger`/`action`/`prompt` required fields on
  `RuleDraft` means `packages/hub/src/db/index.ts` (putRule call site) and `packages/hub/src/rules.test.ts` (draft
  helper) now fail `tsc --noEmit` (verified: `cd packages/hub && npx tsc --noEmit` shows `TS2739`/`TS2322` on those
  two exact lines) until A2/B1 update them — this is the plan's own dependency order (A2, B1 wait on A1). `bun test`
  does not type-check, so runtime suites for hub/dashboard/agent are unaffected: reran all three after the change,
  still "180 pass, 0 fail" / "340 pass, 0 fail" / "181 pass, 0 fail" — identical to baseline.
