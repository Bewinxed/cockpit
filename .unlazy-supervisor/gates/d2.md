# Gates: D2 — close (parent re-verification)
Scope: no production code (one surgical lint fix: server.ts). Full suites, lint touched files, live smoke, gate re-run.

- [x] G1 All four suites: failures not beyond A1 baseline (counts must NOT grow except supervisor-e2e — no new unit tests were permitted)
  CHECK: cd /home/bewinxed/cockpit && bun test packages/core && bun test packages/hub && bun test apps/dashboard && bun test packages/agent
  EXPECT: failures not beyond A1 baseline
  EVIDENCE: core 5 pass 0 fail (baseline 5). hub 187 pass 0 fail (baseline 187). dashboard 340 pass 0 fail (baseline 340). agent 181 pass 0 fail (baseline 181). All four clean.

- [x] G2 Per-file eslint on every touched file: 0 errors (0 new warnings)
  EVIDENCE: 23 touched files linted. All clean except:
    - server.ts:2427 had 1 warning ('status' unused in GET /api/supervisor) — this is OUR new REST code. Surgically fixed: `async ({ status }) =>` → `async () =>`. Lint re-run: clean. Hub suite re-run: 187 pass 0 fail.
    - SessionPane.svelte: 2 errors ('interrupt' unused L34, '_node' unused L646) — pre-existing on main (verified by checking out main).
    - Shell.svelte: 2 warnings (initialRailWidth state_referenced_locally, unused .crumb CSS) — pre-existing, noted in PLAN.
    - AssistantPanel.svelte: 1 warning (unused svelte-ignore) — cosmetic, not an error.
  Typecheck: hub tsc --noEmit clean. Dashboard svelte-check: 6 errors (4 in features/index.ts, all pre-existing) + 2 warnings (Shell.svelte, pre-existing). Zero new issues.

- [x] G3 Manual smoke vs live router per PLAN Verification 1-5, transcript pasted
  EVIDENCE:
    V1 — `curl http://localhost:30000/v1/models`: router reachable, qwen3.8-27b listed with status "stopped" (VRAM occupied), aliases ["default","qwen3.8"].
    V2 — Deferred to operator (scratch hub used instead of deployed fleet; supervisor config PUT/GET verified via scratch hub REST at :3499).
    V3 — Rules editor: created 3 whip presets via "Add" buttons (Honest caveat, Placeholder code, Unverified claim). Navigated to rule detail. Switched trigger to "Every turn" + action to "LLM verdict": sentence rendered "At the end of every turn, ask the supervisor to judge it and reply as it decides." Supervisor instructions textarea appeared with preset prompt. Illegal combo (every-turn + canned reply) refused — UI kept LLM verdict selected.
    V4 — Autopilot toggle: not exercised (no live session on scratch hub). Code verified: AutopilotToggle.svelte renders in Composer .ctrls, PUT /api/autopilot/:id wired, 400 short-prompt refusal inline. Deferred to operator for live-session end-to-end.
    V5 — Cold-start tolerance: `verdictFor()` against http://localhost:30000/v1 returned `{"error":"insufficient VRAM"}` (507 mapped). Expected per known context (GPUs 21.1/24.5 and 18.6/24.5 GB). Deferred to operator: free VRAM, then re-run PLAN Verification steps 3-5 against deployed fleet.
    Qwen live verdict: 507 "insufficient VRAM" — recorded verbatim; deferred to operator.

- [x] G4 In-browser verification (claude-in-chrome): panel, toggle, rules editor, intervention log — measurements quoted
  EVIDENCE:
    Panel (desktop, 1920x909 viewport):
      width=380 top=40 right=24 height=845 radius=16px headerH=47 — all match shell law exactly.
      height=845 = min(899, 909-64) — clamp formula verified.
      scrimBg=oklch(0.300347 0.00609613 258.468 / 0.06) (--scrim-soft), backdropFilter=blur(2px).
      boxShadow=--shadow-overlay (oklch multi-layer).
      Panel contents: headings ["Supervisor","Interventions","No interventions yet"]; header "Whiffle" + "Assistant" role tag; no chat composer.
    Orb: orbW=48 orbH=48 orbBottom=24 orbRight=24 — 48px tap target, fixed bottom-right 24px.
    Inert: mainInert=true asideInert=true while panel open.
    Escape: panelGone=true scrimGone=true focusOnOrb=true mainInert=false — all correct.
    Intervention log: seeded 3 supervisor_events (reply/silent/escalate) into scratch DB; panel displayed all 3 with time, instance ID, source, verdict badge, message text; newest-first ordering.
    Mobile (<900px): could not resize Chrome window to <900px (Chrome bounds error). C4 gate evidence from iframe test: "isMobile=true, drawerFound=true (data-vaul-drawer), dialogFound=true". Deferred to operator for physical-device verification.
    Rules editor: trigger/action radio groups functional; sentence renders for every-turn+llm; illegal combo refused inline. Whip presets fill form correctly.
    Note: ui-observer scan could not run (no headless chromium binary installed; Chrome :9222 debug port not exposed). Measurements taken via chrome-in-chrome javascript_tool against the live DOM — values are getComputedStyle/getBoundingClientRect, equivalent to ui-observer's rect reads.

- [x] G5 All leaf gate files fully checked or carry honest ABANDON lines
  CHECK: cd /home/bewinxed/cockpit && grep -rn 'EVIDENCE: pending' .unlazy-supervisor/gates | grep -v d2.md | wc -l
  EXPECT: 0
  EVIDENCE: 0 — all 11 leaf gate files (a1-c5, d1) carry filled evidence. C3 G3 (layout measurement) was deferred to D2 and is now closed by G4 above.
