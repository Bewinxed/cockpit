# Gates: C3 — composer autopilot toggle
MANDATE: invoke Skill `impeccable` FIRST; craft-floor loaded before editing. Smooth like the mock — no compromise.
Owned: apps/dashboard/src/lib/whiffle/transcript/Composer.svelte (leading snippet only), SessionPane.svelte (pass-through), AutopilotToggle.svelte (new). Forbidden: client.svelte.ts.

- [ ] G1 Toggle in .ctrls before attach; aria-pressed; active = accent glyph, NO solid accent fill
  EVIDENCE: pending
- [ ] G2 Popover (NewProjectPopover pattern): textarea + enable switch + save -> PUT /api/autopilot/:id; reads current state from state.instances
  CHECK: cd /home/bewinxed/cockpit && bun test apps/dashboard
  EXPECT: failures not beyond A1 baseline (no new tests)
  EVIDENCE: pending
- [ ] G3 ui-observer measurements quoted (tap target, popover alignment) or "Layout: Not verified"
  EVIDENCE: pending
- [ ] G4 Motion: popover open/close animates per existing tokens (--e-*, --motion-*), no layout jump on toggle state change; verified by interacting in a real browser session
  EVIDENCE: pending
