# Gates: B2 — hub wiring
Owned: packages/hub/src/server.ts, packages/hub/src/telegram.ts. Forbidden: supervisor.ts internals (bugfixes = report back to B1).

- [ ] G1 Engine constructed beside RuleEngine; observe called right after ruleEngine.observe in the frame path, throw-nothing
  CHECK: cd /home/bewinxed/cockpit && grep -n 'supervisor.observe\|new SupervisorEngine\|createSupervisor' packages/hub/src/server.ts | head
  EXPECT: construct + observe sites
  EVIDENCE: pending
- [ ] G2 REST per C8: GET /api/supervisor (redacted key + live probe), PUT /api/supervisor/config, GET /api/supervisor/events, PUT /api/autopilot/:instanceId (prompt >= 10 chars to enable, publishInstances after); ruleBody widened with defaults
  CHECK: cd /home/bewinxed/cockpit && bun test packages/hub
  EXPECT: failures not beyond A1 baseline; endpoints exercised by curl against a locally started hub, transcripts pasted (no new test files)
  EVIDENCE: pending
- [ ] G3 supervisor_event broadcast mirrors publishDelegateEvent; instances rows carry autopilot additively
  EVIDENCE: pending
- [ ] G4 telegram.onSupervisor: header+reason+clip 900+dashboard link, sent AND tracked (reply routes via talkBack); permission machinery untouched (no diffs in pending.ts / onAsk / RESOLVE_PERMISSION paths)
  CHECK: cd /home/bewinxed/cockpit && git diff --stat packages/hub/src/pending.ts
  EXPECT: empty
  EVIDENCE: pending
- [ ] G5 HubServices/RegistryShape untouched
  CHECK: cd /home/bewinxed/cockpit && bun test apps/dashboard/src/lib/whiffle/stream-e2e.test.ts
  EXPECT: all pass, zero edits to that test file
  EVIDENCE: pending
