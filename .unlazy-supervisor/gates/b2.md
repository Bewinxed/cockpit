# Gates: B2 — hub wiring
Owned: packages/hub/src/server.ts, packages/hub/src/telegram.ts. Forbidden: supervisor.ts internals (bugfixes = report back to B1).

- [x] G1 Engine constructed beside RuleEngine; observe called right after ruleEngine.observe in the frame path, throw-nothing
  CHECK: cd /home/bewinxed/cockpit && grep -n 'supervisor.observe\|new SupervisorEngine\|createSupervisor' packages/hub/src/server.ts | head
  EXPECT: construct + observe sites
  EVIDENCE: server.ts:856 `new SupervisorEngine({...})` with deps db, agent, telegram, publish; server.ts:3596 `supervisor.observe(...)` right after `ruleEngine.observe(...)` in the `kind === 'frame'` block. Both inside the same throw-nothing discipline (engine's own try/catch in observe).

- [x] G2 REST per C8: GET /api/supervisor (redacted key + live probe), PUT /api/supervisor/config, GET /api/supervisor/events, PUT /api/autopilot/:instanceId (prompt >= 10 chars to enable, publishInstances after); ruleBody widened with defaults
  CHECK: cd /home/bewinxed/cockpit && bun test packages/hub
  EXPECT: failures not beyond A1 baseline; endpoints exercised by curl against a locally started hub, transcripts pasted (no new test files)
  EVIDENCE:
    bun test packages/hub → 180 pass, 0 fail (baseline held)
    npx tsc --noEmit → clean
    ruleBody widened: trigger (optional, default 'pattern'), action (optional, default 'reply'), prompt (optional, default null)
    Curl transcripts (scratch hub port 39747, scratch DB, no telegram):

    GET /api/supervisor (unconfigured):
      {"config":{"enabled":false,"baseUrl":null,"model":null},"status":{"configured":false}}

    PUT /api/supervisor/config {"enabled":true,"baseUrl":"http://localhost:30000","model":"qwen3.8"}:
      {"ok":true}

    GET /api/supervisor (configured, live probe hit running router):
      {"config":{"enabled":true,"baseUrl":"http://localhost:30000","model":"qwen3.8"},
       "status":{"configured":true,"reachable":true,"resolvedModel":"qwen3.8-27b"}}

    GET /api/supervisor/events:
      []

    PUT /api/autopilot/test-inst-001 {"enabled":true,"prompt":"Watch for false done claims and demand test output."}:
      HTTP 200 {"ok":true}
      Instance autopilot field verified: {"enabled":true,"prompt":"Watch for false done claims and demand test output.","updatedAt":1788256207983}

    PUT /api/autopilot/test-inst-001 {"enabled":true,"prompt":"short"}:
      HTTP 400 "A standing prompt of under ten characters is not one — say what the autopilot should watch for."

    GET /api/supervisor/events?limit=5:
      []

    apiKey is never serialized in GET /api/supervisor response (config object contains only enabled, baseUrl, model — no apiKey field).

- [x] G3 supervisor_event broadcast mirrors publishDelegateEvent; instances rows carry autopilot additively
  EVIDENCE: publishSupervisorEvent (server.ts:838) broadcasts `{verb:'frames', machineId, instanceId, payload:{kind:'supervisor_event', instanceId, event}}` — same envelope shape as publishDelegateEvent (server.ts:1348). Autopilot flows through listInstances (db/index.ts:1641 `.map(row => ({...row, ...(row.autopilot ? {autopilot: row.autopilot} : {})}))`); verified via curl: instance.autopilot present after PUT.

- [x] G4 telegram.onSupervisor: header+reason+clip 900+dashboard link, sent AND tracked (reply routes via talkBack); permission machinery untouched (no diffs in pending.ts / onAsk / RESOLVE_PERMISSION paths)
  CHECK: cd /home/bewinxed/cockpit && git diff --stat packages/hub/src/pending.ts
  EXPECT: empty
  EVIDENCE:
    git diff --stat packages/hub/src/pending.ts → empty (no diffs)
    onSupervisor implementation (telegram.ts:453-466): header with 🤖 emoji, message clipped at 900 chars, dashboard session link, sent via send(), tracked via track(sent.message_id, {instanceId, machineId, text}).
    talkBack wiring verified: track registers in the tracked map; onMessage (telegram.ts:525) looks up tracked.get(repliedTo) (line 530); when not a pending permission, calls talkBack(entry, text, attached) (line 580) which sends into the session via agent socket with origin:{kind:'human'}. No new code needed for this path.
    onAsk, RESOLVE_PERMISSION paths: zero edits.

- [x] G5 HubServices/RegistryShape untouched
  CHECK: cd /home/bewinxed/cockpit && bun test apps/dashboard/src/lib/whiffle/stream-e2e.test.ts
  EXPECT: all pass, zero edits to that test file
  EVIDENCE: bun test apps/dashboard/src/lib/whiffle/stream-e2e.test.ts → 22 pass, 0 fail (94 expect() calls). HubServices interface unchanged; RegistryShape unchanged; zero edits to stream-e2e.test.ts.
