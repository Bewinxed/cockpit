# Gates: B1 — SupervisorEngine
Owned: packages/hub/src/supervisor.ts (new), packages/hub/src/rules.ts (reload filter only). Forbidden: server.ts, telegram.ts, db files. POLICY: no new tests — correctness rides D1's e2e + D2 live smoke; your gate is compile/lint/suite + a self-review pass.

- [ ] G1 Engine per C3 complete: observe buffers (spoken/parent_tool_use_id rules), result schedules, aborted flushes; evaluation fully off the frame path; selection (autopilot > first in-scope llm rule via core ruleInScope/ruleMatches)
  CHECK: cd /home/bewinxed/cockpit && npx eslint packages/hub/src/supervisor.ts
  EXPECT: 0 errors
  EVIDENCE: pending
- [ ] G2 All four loop guards implemented: one-in-flight drop; attribution flag consecutive++/reset; cap 3 -> forced escalate + mute + human-turn reset; staleness drop. Adversarial self-review pass performed and findings fixed (list them)
  EVIDENCE: pending
- [ ] G3 Delivery: RuleEngine-identical envelope, shouldQuery true, origin system supervisor:*, body verbatim; unreachable -> skipped(unreachable); noteRuleFire + RULE_FIRE_CEILING; error verdict on bad output = zero session traffic
  EVIDENCE: pending (code refs)
- [ ] G4 RuleEngine.reload filters to action==='reply'; existing suite unbroken + typecheck clean
  CHECK: cd /home/bewinxed/cockpit && bun test packages/hub
  EXPECT: failures not beyond A1 baseline
  EVIDENCE: pending
