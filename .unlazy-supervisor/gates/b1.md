# Gates: B1 — SupervisorEngine
Owned: packages/hub/src/supervisor.ts (new), packages/hub/src/rules.ts (reload filter only). Forbidden: server.ts, telegram.ts, db files. POLICY: no new tests — correctness rides D1's e2e + D2 live smoke; your gate is compile/lint/suite + a self-review pass.

- [x] G1 Engine per C3 complete: observe buffers (spoken/parent_tool_use_id rules), result schedules, aborted flushes; evaluation fully off the frame path; selection (autopilot > first in-scope llm rule via core ruleInScope/ruleMatches)
  CHECK: cd /home/bewinxed/cockpit && npx eslint packages/hub/src/supervisor.ts
  EXPECT: 0 errors
  EVIDENCE: lint clean (0 output), tsc --noEmit clean. observe() wraps #observe in try/catch (L182-187), #observe never awaits (L228: void this.#evaluate().catch(() => {})), spoken() extracts text blocks (L127-131), parent_tool_use_id check (L193), aborted flush (L203-208), selection priority autopilot > llm rule (L502-504, L508-522).
- [x] G2 All four loop guards implemented: one-in-flight drop; attribution flag consecutive++/reset; cap 3 -> forced escalate + mute + human-turn reset; staleness drop. Adversarial self-review pass performed and findings fixed (list them)
  EVIDENCE: (1) in-flight: L268-276. (2) attribution: initiatedTurn consumed at result L221-227, consecutive++ on self-initiated, reset to 0 + unmute on human. (3) cap: L375-379, SUPERVISOR_CONSECUTIVE_MAX=3 (PLAN C3), muted=true, forced escalate. (4) staleness: L339-349, resultTimestamp compared.
  SELF-REVIEW FINDINGS:
  - FOUND+FIXED: #resolveConfig let DB enabled:false fall through to env vars, re-enabling supervisor. Fixed: DB row present → authoritative (even disabled); env only when no row exists.
  - FOUND+ACCEPTED: In-flight/muted skip events hardcode source:'autopilot' because selection hasn't run yet. Cosmetic — these are 'skipped' verdicts, source is diagnostic. DB requires a value.
  - VERIFIED: inFlight cleared in finally block (L486-491) as safety net; no leak path — acquire() can't throw, verdictFor never throws, all try-block paths clear inFlight before return.
  - VERIFIED: Semaphore release in finally (L487) only runs when acquire() returned true (try block only entered after acquire). No double-release or missed-release path.
  - VERIFIED: muted reset at L226 fires on every human-initiated turn (initiatedTurn=false path). No stuck-mute path.
- [x] G3 Delivery: RuleEngine-identical envelope, shouldQuery true, origin system supervisor:*, body verbatim; unreachable -> skipped(unreachable); noteRuleFire + RULE_FIRE_CEILING; error verdict on bad output = zero session traffic
  EVIDENCE: Envelope at L418-433 matches RuleEngine.#fire shape (rules.ts:284-304): verb:'send', payload.message.shouldQuery:true, payload.urgent:false, origin {kind:'system', name:'supervisor:*'}, body = verdict.message verbatim (L426). Unreachable: L397-411. noteRuleFire: L439-443, L465-469, respects RULE_FIRE_CEILING. Error verdict: L351-361, records event, no sender.send call. Opacity verified: origin metadata does not alter model-visible input (evidence: packages/agent/src/harnesses/claude.ts:828,851,875 — origin is only read by isInjected() for frame echo and queue announcement decisions; the SDK message passed to the model carries only role+content, origin is whiffle-side metadata never serialized to the API).
- [x] G4 RuleEngine.reload filters to action==='reply'; existing suite unbroken + typecheck clean
  CHECK: cd /home/bewinxed/cockpit && bun test packages/hub
  EXPECT: failures not beyond A1 baseline
  EVIDENCE: reload filter at rules.ts:93 — `.filter((rule) => rule.enabled && rule.action === 'reply')`. Suite: 180 pass, 0 fail (baseline: 180 pass, 0 fail). tsc --noEmit clean.
- [x] G5 Opacity verified: origin metadata does not alter model-visible input (evidence: packages/agent/src/harnesses/claude.ts:828,851,875)
  EVIDENCE: Traced send path: hub → agent → claude.ts send() (L813). The NeutralUserMessage is cast to SDKUserMessage (L815: `const sdk = message as unknown as SDKUserMessage`), then pushed to the input queue. The Claude SDK reads only `role` and `content` from the user message for the API payload. `origin` is read solely by `isInjected()` (L828,851,875) to decide: (a) whether to echo the message as a frame back to dashboards, (b) whether to announce it as a queued message. Neither path serializes origin into model-visible bytes. The model sees `message.content` verbatim — the supervisor's reply text — with no header, wrapper, or origin signal. Using `kind:'system'` is safe for dashboard labeling.
