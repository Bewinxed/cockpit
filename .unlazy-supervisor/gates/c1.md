# Gates: C1 — dashboard plumbing
Owned: apps/dashboard/src/lib/whiffle/client.svelte.ts, frames.ts, frames.test.ts, autopilot.ts (new), supervisor.ts (new). Forbidden: components, routes.

- [x] G1 supervisor_event frames ingested structurally (delegateEventOf idiom); state.supervisorEvents ring capped 200 (no new tests; existing suite only)
  CHECK: cd /home/bewinxed/cockpit && bun test apps/dashboard
  EXPECT: failures not beyond A1 baseline
  EVIDENCE: 340 pass, 0 fail, 871 expect() calls — baseline preserved
- [x] G2 ruleLabel: supervisor:autopilot -> "Autopilot"; supervisor:<rule> -> "Supervisor — <rule>" (verified later in-browser at D2; no new test cases)
  CHECK: cd /home/bewinxed/cockpit && bun test apps/dashboard/src/lib/whiffle/frames.test.ts
  EXPECT: existing cases stay green
  EVIDENCE: 74 pass, 0 fail, 187 expect() calls
- [x] G3 autopilot.ts + supervisor.ts fetch wrappers follow rules.ts idiom (typed, error-surfaced)
  EVIDENCE: both files follow rules.ts `said`/`send`/`json` pattern; typecheck 0 errors; eslint clean
