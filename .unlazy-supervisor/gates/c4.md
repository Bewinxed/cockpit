# Gates: C4 — assistant panel + orb
MANDATE: invoke Skill `impeccable` FIRST; craft-floor loaded before editing. Smooth like the mock — no compromise: the mock's shell + feel (mocks/v5-assistant.html, v3's modal JS) is the acceptance bar.
Owned: apps/dashboard/src/lib/whiffle/assistant/* (new), Shell.svelte (mount only). Forbidden: client.svelte.ts.

- [ ] G1 Shell law: width 380, top 40 / right 24, radius 16, header 47, scrim var(--scrim-soft), height min(899px, 100dvh - 64px); tokens only, zero new hues; orb is the ONLY solid --accent-solid fill
  EVIDENCE: pending (ui-observer numbers)
- [ ] G2 Contents: supervisor status (GET /api/supervisor), focused session autopilot state, live intervention log (REST seed + supervisor_event frames, newest first, click navigates); NO chat composer
  CHECK: cd /home/bewinxed/cockpit && bun test apps/dashboard
  EXPECT: failures not beyond A1 baseline (no new tests)
  EVIDENCE: pending
- [ ] G3 Sub-900px: full-bleed sheet, role=dialog, aria-modal, background inert, Escape closes, focus returns to orb (v3 syncModal ported)
  EVIDENCE: pending
- [ ] G4 Motion: panel enters over var(--scrim-soft) at --motion-base (DESIGN.md L81 — the summon is one of the three expressive moments); enter/exit smooth in a real browser (no flash, no reflow jump, reduced-motion honored); orb press states polished
  EVIDENCE: pending (browser verification)
