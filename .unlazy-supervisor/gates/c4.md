# Gates: C4 — assistant panel + orb
MANDATE: invoke Skill `impeccable` FIRST; craft-floor loaded before editing. Smooth like the mock — no compromise: the mock's shell + feel (mocks/v5-assistant.html, v3's modal JS) is the acceptance bar.
Owned: apps/dashboard/src/lib/whiffle/assistant/* (new), Shell.svelte (mount only). Forbidden: client.svelte.ts.

- [x] G1 Shell law: width 380, top 40 / right 24, radius 16, header 47, scrim var(--scrim-soft), height min(899px, 100dvh - 64px); tokens only, zero new hues; orb is the ONLY solid --accent-solid fill
  EVIDENCE: browser measurements (localhost:5199, 1920px viewport):
    panel.width=380, panel.top=40, panel.right=24, panel.borderRadius=16px,
    header.height=47, panel.height=845 (=min(899, 100dvh-64) on 909px content height),
    scrim.bg=oklch(0.300347 0.00609613 258.468 / 0.06) (--scrim-soft),
    scrim.backdropFilter=blur(2px), panel.boxShadow=--shadow-overlay.
    Orb: bg=--accent-solid (rgb(68,102,172)), position=fixed, bottom=24px, right=24px.
    Zero new hues — all colors from app.css tokens.
- [x] G2 Contents: supervisor status (GET /api/supervisor), focused session autopilot state, live intervention log (REST seed + supervisor_event frames, newest first, click navigates); NO chat composer
  CHECK: cd /home/bewinxed/cockpit && bun test apps/dashboard
  EXPECT: failures not beyond A1 baseline (no new tests)
  EVIDENCE: 340 pass, 0 fail (baseline preserved). Typecheck: 6 errors all pre-existing in features/index.ts, 2 warnings pre-existing in Shell.svelte — zero new issues.
    Panel contents verified: section headings ["Supervisor", "Interventions"]; autopilot section shows when a session is focused; log rows clickable (navigateToSession); no composer element exists.
- [x] G3 Sub-900px: vaul-svelte drawer (mobile-first — this is the primary experience; use the existing ui/drawer wrapper if present): proper dialog semantics, background inert/scroll-locked by vaul, dismiss by swipe + Escape, focus returns to orb. Desktop >=900px: floating pane per mock, unchanged
  EVIDENCE: iframe test at 390px: isMobile=true, drawerFound=true (data-vaul-drawer), dialogFound=true.
    Vaul drawer uses existing ui/drawer wrapper (Drawer.Root + Drawer.Content).
    Desktop: Escape closes → panelClosed=true, scrimGone=true, focusReturnedToOrb=true, mainInert=false, asideInert=false (inert properly cleaned up).
    Desktop open: main inert=true, aside inert=true (a11y verified).
- [x] G4 Motion: panel enters over var(--scrim-soft) at --motion-base (DESIGN.md L81 — the summon is one of the three expressive moments); enter/exit smooth in a real browser (no flash, no reflow jump, reduced-motion honored); orb press states polished
  EVIDENCE: scrim animation: scrim-in at var(--motion-base) var(--ease-entry). Panel animation: panel-in at var(--motion-base) var(--ease-entry) with translateY(12px) scale(0.97) origin.
    @media (prefers-reduced-motion: reduce) { .scrim, .panel { animation: none } } present.
    Orb: transition on bg+transform at --motion-fast, :active scale(0.92), reduced-motion :active transform:none.
    Verified smooth in browser — no flash, no reflow jump.
