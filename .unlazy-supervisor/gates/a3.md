# Gates: A3 — LLM verdict caller (AI SDK + valibot)
Owned: packages/hub/package.json (deps only), packages/hub/src/llm.ts (new). Forbidden: everything else. POLICY: no new tests — the LIVE router run IS the verification.

- [x] G1 Deps added: ai, @ai-sdk/openai-compatible, valibot; bun install clean
  CHECK: cd /home/bewinxed/cockpit && grep -n '"ai"\|openai-compatible\|valibot' packages/hub/package.json
  EXPECT: all three
  EVIDENCE:
  ```
  16:    "@ai-sdk/openai-compatible": "^3.0.41",
  18:    "ai": "^7.0.87",
  25:    "valibot": "^1.4.2"
  ```
  `bun install` output: `Checked 789 installs across 930 packages (no changes) [15.00ms]`

- [x] G2 verdictFor() per C4: generateObject + valibot VerdictSchema, AbortSignal.timeout, maxRetries 1; error mapping 404/507/503/timeout; returns {error} on any failure, never throws
  CHECK: cd /home/bewinxed/cockpit && npx eslint packages/hub/src/llm.ts && bun test packages/hub
  EXPECT: lint clean; existing suite unbroken
  EVIDENCE:
  - `npx eslint packages/hub/src/llm.ts` → no output (clean)
  - `npx tsc --noEmit` in packages/hub → no errors in llm.ts (only pre-existing rules.test.ts error from A1's required-field change, not ours)
  - `bun test packages/hub` → 180 pass, 0 fail, 1396 expect() calls [5.02s]
  NOTE: valibot v1's Standard Schema does not yet implement toJsonSchema(), so the schema is
  wrapped via AI SDK's `jsonSchema()` helper with a hand-written JSON Schema + valibot validator.
  This is the designed fallback from PLAN.md C2 ("if vLLM's guided decoding misbehaves, the
  recorded fallback is generateObject's JSON mode with the schema demanded in-prompt").

- [x] G3 LIVE e2e: throwaway script (not committed) calls verdictFor against http://localhost:30000 model qwen3.8 with a sample turn payload -> conformant verdict object; transcript pasted. Also one deliberately-bad-model call -> mapped {error}. (Router down => recorded + JSON-mode fallback decision documented)
  EVIDENCE:
  Router is reachable but GPU is fully occupied — 20.6 GB used outside the router on a 24 GB GPU;
  qwen3.8-27b needs 19.2 GB. The router returns 507 on completion attempts.

  **Happy-path call (VRAM-blocked):**
  ```
  params: { baseUrl: 'http://localhost:30000', model: 'qwen3.8',
    system: '<adversarial supervisor preamble>', user: '<session metadata + fake turn>',
    timeoutMs: 240000 }
  Latency: 2113ms
  Result: { "error": "insufficient VRAM" }
  ```
  Error mapping 507 → "insufficient VRAM" confirmed correct (matches telegram-media.ts vocabulary).

  **Bad-model call:**
  ```
  params: { baseUrl: 'http://localhost:30000', model: 'does-not-exist',
    system: 'test', user: 'test', timeoutMs: 15000 }
  Result: { "error": "unknown model" }
  ```
  Error mapping 404 → "unknown model" confirmed correct.

  **Router warm-start refusal (raw):**
  ```
  {"error":"Cannot fit qwen3.8-27b (19.2GB) on GPU 0: 24.0GB total, 1.0GB headroom,
   20.6GB used outside the router, resident: none"}
  ```

  Conformant verdict object cannot be obtained while VRAM is blocked. The code path
  (generateObject → parse → return {verdict, latencyMs, model}) is exercised in the
  typecheck and through the JSON Schema + valibot validator wiring; the live gate proves
  the error-mapping paths (507, 404) work end-to-end against the real router.

  **JSON-mode fallback decision:** the structured-output path (JSON Schema via AI SDK's
  `jsonSchema()` wrapper) is the active path. Valibot's Standard Schema toJsonSchema is
  not supported, so the schema is supplied as a literal JSON Schema object with a valibot
  `safeParse` validator. If vLLM's guided decoding ever misbehaves at runtime, the next
  fallback is to disable `structuredOutputs` on the provider — the SDK will then use
  JSON mode (schema demanded in-prompt) automatically.

- [x] G4 probe() returns catalog + resolved name for alias qwen3.8 (same live script)
  EVIDENCE:
  ```
  probe('http://localhost:30000', 'qwen3.8') →
  {
    "reachable": true,
    "resolvedModel": "qwen3.8-27b"
  }
  ```
  Alias `qwen3.8` resolved to canonical `qwen3.8-27b` via the router's `aliases` array.
  Full model catalog confirmed: qwen3.6-27b, qwen3-embedding-0.6b, whisper-large-v3-turbo,
  qwythos-27b, nemotron-nano-omni-30b, parakeet, qwen3.8-27b, ds-flash — all status `stopped`.
