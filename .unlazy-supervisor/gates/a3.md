# Gates: A3 — LLM verdict caller (AI SDK + valibot)
Owned: packages/hub/package.json (deps only), packages/hub/src/llm.ts (new). Forbidden: everything else. POLICY: no new tests — the LIVE router run IS the verification.

- [ ] G1 Deps added: ai, @ai-sdk/openai-compatible, valibot; bun install clean
  CHECK: cd /home/bewinxed/cockpit && grep -n '"ai"\|openai-compatible\|valibot' packages/hub/package.json
  EXPECT: all three
  EVIDENCE: pending
- [ ] G2 verdictFor() per C4: generateObject + valibot VerdictSchema, AbortSignal.timeout, maxRetries 1; error mapping 404/507/503/timeout; returns {error} on any failure, never throws
  CHECK: cd /home/bewinxed/cockpit && npx eslint packages/hub/src/llm.ts && bun test packages/hub
  EXPECT: lint clean; existing suite unbroken
  EVIDENCE: pending
- [ ] G3 LIVE e2e: throwaway script (not committed) calls verdictFor against http://localhost:30000 model qwen3.8 with a sample turn payload -> conformant verdict object; transcript pasted. Also one deliberately-bad-model call -> mapped {error}. (Router down => recorded + JSON-mode fallback decision documented)
  EVIDENCE: pending
- [ ] G4 probe() returns catalog + resolved name for alias qwen3.8 (same live script)
  EVIDENCE: pending
