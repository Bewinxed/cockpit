# Gates: C2 — rules editor + whip presets
Owned: apps/dashboard/src/routes/rules/+page.svelte, routes/rules/[id]/+page.svelte, lib/whiffle/rules.ts. Forbidden: client.svelte.ts.

- [x] G1 Editor exposes trigger/action/prompt; illegal combos surface ruleProblem sentences; every-turn hides pattern fields; llm hides reply field
  CHECK: cd /home/bewinxed/cockpit && bun test apps/dashboard
  EXPECT: failures not beyond A1 baseline (no new tests)
  EVIDENCE: 340 pass 0 fail (baseline maintained); typecheck 0 errors 2 pre-existing warnings
- [x] G2 ruleSentence for an every-turn llm rule reads correctly; quoted in report
  EVIDENCE: `At the end of every turn, ask the supervisor to judge it and reply as it decides.`
- [x] G3 WHIP_PRESETS: 4 templates (done-claim, stall, exercise-left, out-of-scope files); click fills the form and validates
  EVIDENCE: 4 presets exported; usePreset() fills draft with trigger/action/prompt/timing/requireAck; ruleProblem({...preset, name:'x', enabled:true, prompt:preset.prompt, timing:'turn', requireAck:false, scope:{}}) returns {} (valid)
- [ ] G4 Layout measured (ui-observer) or "Layout: Not verified" + reason
  EVIDENCE: Layout: Not verified — leaf D2 does the full in-browser pass per operator testing policy
