# fn-15.2 @cockpit/hub: machine-scoped control routing, control_result reply-to-sender, instance rows on resume

## Description
TBD

## Acceptance
- [ ] TBD

## Done summary
Machine-scoped session ops via control (5-fn whitelist), real resume verified by transcript recall, fork flags reach CLI. control_result reply-to-sender with TTL'd requester map; Elysia2 ws.id keying bug fixed; resume-aware instance upsert; unknown status on agent drop.
## Evidence
- Commits:
- Tests:
- PRs: