# fn-14.1 @cockpit/agent: session supervisor — spawn/send/stop/control, query() + input stream, frames pump, permission relay via requestId (+thin payload types in core)

## Description
TBD

## Acceptance
- [ ] TBD

## Done summary
Session supervisor live-verified: real SDK 0.3.220 query() through daemon (pong round trip, canUseTool permission parked+resolved via control resolvePermission, tool executed, clean stop, no orphans, 23 partial frames). Thin payload types added to core.
## Evidence
- Commits:
- Tests:
- PRs: