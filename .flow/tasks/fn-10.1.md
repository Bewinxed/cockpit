# fn-10.1 Shared event types in @cockpit/core

## Description
Move event type definitions from dashboard to `@cockpit/core` so both hub-server and dashboard share the same types.

## Current State
- Event types defined in `/home/user/cockpit/apps/dashboard/src/lib/stores/sse-events.ts`
- `CockpitEventMap`, `CockpitEventType`, event interfaces

## Tasks
1. Create `packages/core/src/events/` directory
2. Move event interfaces from `sse-events.ts` to `packages/core/src/events/dashboard.ts`
3. Export from `packages/core/src/index.ts`
4. Update dashboard imports to use `@cockpit/core`
5. Add WebSocket-specific message types (request/response wrappers)

## Files to Modify
- `packages/core/src/events/dashboard.ts` (CREATE)
- `packages/core/src/index.ts` (MODIFY - add exports)
- `apps/dashboard/src/lib/stores/sse-events.ts` (MODIFY - re-export from core)
- `apps/dashboard/src/lib/stores/index.svelte.ts` (MODIFY - update imports)
## Acceptance
- [ ] Event types exported from `@cockpit/core`
- [ ] Dashboard imports from `@cockpit/core` without errors
- [ ] Hub-server can import same types
- [ ] No TypeScript errors in either package
- [ ] `bun run build` succeeds
## Done summary
## Summary

Created shared dashboard event types in `@cockpit/core/dashboard`:

**New files:**
- `packages/core/src/dashboard/types.ts` - All dashboard event interfaces (agent, instance, task, permission, question, project events)
- `packages/core/src/dashboard/river.ts` - Pre-built `dashboardEvents` schema using river.ts with request/response support for RPC commands
- `packages/core/src/dashboard/index.ts` - Clean re-exports

**Key design decisions:**
- Namespace as `@cockpit/core/dashboard` to avoid conflicts with `@cockpit/core/protocol` (agent↔hub JSON-RPC types)
- No backwards compatibility cruft - fresh river.ts-first design
- Pre-built `dashboardEvents` schema ready for both SSE (current) and WebSocket (fn-10.2+)
- Request/response types defined for commands: spawn, send, stop, permission.response, question.response

**Updated files:**
- Deleted `apps/dashboard/src/lib/stores/sse-events.ts`
- Updated all dashboard stores to import from `@cockpit/core/dashboard`
- `index.svelte.ts` now uses `dashboardEvents` from core instead of inline definitions
## Evidence
- Commits:
- Tests: bun run build succeeded
- PRs: