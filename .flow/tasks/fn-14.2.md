# fn-14.2 @cockpit/hub: dashboard WS endpoint, frame fan-out, verb routing by machineId, self-initializing DB, /api reads, pending-permission cache

## Description
TBD

## Acceptance
- [ ] TBD

## Done summary
Hub relay live-verified: /ws + /ws/dashboard, verb routing by machineId, frames fan-out, pending-permission cache lifecycle, self-initializing DB (fresh boot + re-migration), /api reads, offline marking on disconnect.
## Evidence
- Commits:
- Tests:
- PRs: