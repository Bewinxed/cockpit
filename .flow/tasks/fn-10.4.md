# fn-10.4 Dashboard WebSocket client

## Description
Replace the SSE `RiverClient` with a native WebSocket client in the dashboard.

## Current State
- `/home/user/cockpit/apps/dashboard/src/lib/stores/index.svelte.ts` uses `RiverClient` for SSE

## Tasks
1. Create `apps/dashboard/src/lib/stores/websocket.svelte.ts`
2. Implement WebSocket client with:
   - Connection state management (`connecting`, `connected`, `disconnected`, `error`)
   - JSON-RPC message parsing
   - Event routing to existing store handlers
   - Reconnection with exponential backoff (reuse existing logic)
3. Update `index.svelte.ts` to use new WS client instead of SSE
4. Keep HMR-persistent pattern (`globalThis.__wsClient`)

## Message Handling
```typescript
// Receive notification
{ jsonrpc: "2.0", method: "sdk:message", params: {...} }
// Route to: handleSdkMessage(params)

// Receive response to our request
{ jsonrpc: "2.0", id: "req-123", result: {...} }
// Resolve pending promise
```

## Files
- `apps/dashboard/src/lib/stores/websocket.svelte.ts` (CREATE)
- `apps/dashboard/src/lib/stores/index.svelte.ts` (MODIFY - switch to WS)
## Acceptance
- [ ] Dashboard connects via WebSocket
- [ ] Connection state shown in UI (if applicable)
- [ ] All existing events (agent, instance, sdk:message, etc.) handled correctly
- [ ] Reconnection works with backoff
- [ ] HMR doesn't break connection state
- [ ] No TypeScript errors
## Done summary
## Summary

Replaced SSE `RiverClient` with WebSocket `RiverSocketAdapter` in the dashboard:

**Modified file:** `apps/dashboard/src/lib/stores/index.svelte.ts`

**Changes:**
- Imported `RiverSocketAdapter` from `river.ts/websocket` instead of `RiverClient`
- Updated HMR-persistent globals: `__wsSocket`, `__wsAdapter` (renamed from SSE variants)
- Created `setupEventHandlers(adapter)` to wire all event handlers to the adapter
- `setupWSAndConnect(baseUrl)`:
  - Converts http(s) URL to ws(s) URL
  - Creates native `WebSocket` to `/ws/dashboard`
  - Creates `RiverSocketAdapter` with `dashboardEvents` schema
  - Wires `ws.onmessage` → `adapter.handleMessage()`
  - Handles reconnection with exponential backoff
- Added legacy aliases for migration (`setupSSEAndConnect`, `disconnectSSE`, `reconnectSSE`)

**Connection flow:**
1. `setupWSAndConnect()` called from layout
2. WebSocket connects to hub `/ws/dashboard`
3. Hub sends `connected` event with `clientId`
4. All events flow through `RiverSocketAdapter` handlers
## Evidence
- Commits:
- Tests: IDE diagnostics clean - no type errors
- PRs: