# fn-13.1 Move packages/core to packages/legacy-core; scaffold @cockpit/core (SDK 0.3.220 re-exports + tunnel envelope)

## Description
TBD

## Acceptance
- [ ] TBD

## Done summary
Moved packages/core -> packages/legacy-core (git mv, name-based imports unaffected; root tsconfig reference updated, legacy typecheck baseline unchanged). Scaffolded @cockpit/core: SDK pinned exactly 0.3.220, type-only SDK re-exports, 8-verb tunnel Envelope, COCKPIT_HUB_PORT/COCKPIT_ENV constants. packages/core typecheck exit 0; legacy + new resolution verified.
## Evidence
- Commits:
- Tests:
- PRs: