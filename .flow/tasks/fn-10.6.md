# fn-10.6 Remove BroadcastService and SSE endpoint

## Description
Remove the old SSE infrastructure now that WebSocket is working.

## Files to Remove/Modify
1. **Remove** `packages/hub-server/src/services/broadcast.ts`
2. **Remove** `packages/hub-server/src/api/events.ts` (SSE endpoint)
3. **Update** `packages/hub-server/src/services/index.ts` (remove exports)
4. **Update** `packages/hub-server/src/index.ts` (remove SSE routes)
5. **Update** all files that call `getBroadcastService().broadcast()` to use `getDashboardRegistry().broadcast()`

## Broadcast Call Locations (28 total)
- `packages/hub-server/src/api/websocket.ts` (13 calls)
- `packages/hub-server/src/api/instances.ts` (6 calls)
- `packages/hub-server/src/api/projects.ts` (3 calls)
- `packages/hub-server/src/api/events.ts` (5 calls - being deleted)

## Dashboard Cleanup
1. **Remove** SSE-related code from `apps/dashboard/src/lib/stores/index.svelte.ts`
2. **Remove** or repurpose `apps/dashboard/src/lib/stores/sse-events.ts`
## Acceptance
- [ ] `BroadcastService` file deleted
- [ ] SSE `/api/events` endpoint removed
- [ ] All broadcast calls use `DashboardRegistry`
- [ ] No references to `getBroadcastService()` remain
- [ ] No SSE-related code in dashboard
- [ ] `bun run build` succeeds
- [ ] Dashboard fully functional via WebSocket only
## Done summary
## Summary

Removed all SSE infrastructure and migrated to WebSocket-only real-time communication.

### Hub Server Changes

**Deleted files:**
- `packages/hub-server/src/services/broadcast.ts` - SSE BroadcastService
- `packages/hub-server/src/api/events.ts` - SSE endpoints

**Updated files:**
- `packages/hub-server/src/api/websocket.ts` - Changed all `getBroadcastService()` → `getDashboardRegistry()`
- `packages/hub-server/src/api/dashboard-ws.ts` - Removed BroadcastService import
- `packages/hub-server/src/api/instances.ts` - Changed to DashboardRegistry
- `packages/hub-server/src/api/projects.ts` - Changed to DashboardRegistry
- `packages/hub-server/src/services/index.ts` - Removed BroadcastService exports
- `packages/hub-server/src/api/index.ts` - Removed SSE route exports
- `packages/hub-server/src/index.ts` - Removed SSE routes, updated startup log

### Dashboard Changes

**Deleted files:**
- `apps/dashboard/src/routes/api/events/+server.ts` - SSE proxy route

**Updated files:**
- `apps/dashboard/src/lib/config.ts` - Renamed SSE config to WS config
- `apps/dashboard/src/routes/+layout.svelte` - Use `setupWSAndConnect`/`disconnectWS` directly
- `apps/dashboard/src/lib/stores/index.svelte.ts` - Removed legacy SSE aliases

### Result
- All real-time events now flow through WebSocket (`/ws/dashboard`)
- Commands use river.ts `request()` for RPC semantics
- No SSE code remains in production paths
## Evidence
- Commits:
- Tests: IDE diagnostics clean - no type errors
- PRs: