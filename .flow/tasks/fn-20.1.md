# fn-20.1 Message width contract + bits-ui transcript primitives

## Description
TBD

## Acceptance
- [ ] TBD

## Done summary
# fn-20.1 — Message width contract + bits-ui transcript primitives

Commit da4b7b5. Bubble wrapper gained max-w-full min-w-0, fixing the clipped
full-width user-message bug (verified on the offending transcript: widest
bubble 762px within the 896px column's 85% cap, horizontal overflow 0).
ChatMessage tool/hook collapses migrated to the shared Collapsible; HelpMenu
tablist → bits-ui Tabs; DiffModal + JumpPalette → bits-ui Dialog with exit
animations preserved via onOpenChangeComplete; shared collapsible retimed to
250/180 easeOutQuint and made inert when closed. Gates: svelte-check 0/0,
eslint clean, bun run build succeeds. Jump palette verified in Chrome
(open, autofocus, Escape close).
## Evidence
- Commits:
- Tests:
- PRs: