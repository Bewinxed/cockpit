# fn-10.3 Dashboard client registry in hub

## Description
Create a dashboard client registry similar to `AgentRegistry` but for dashboard WebSocket clients.

## Pattern to Follow
See `/home/user/cockpit/packages/hub-server/src/services/agent-registry.ts`

## Tasks
1. Create `packages/hub-server/src/services/dashboard-registry.ts`
2. Implement `DashboardRegistry` class with:
   - `clients: Map<string, DashboardClient>`
   - `register(ws, clientId)` - add client on connect
   - `unregister(clientId)` - remove on disconnect
   - `broadcast(method, params)` - send to all clients
   - `sendToClient(clientId, method, params)` - send to specific
3. Use HMR-persistent singleton pattern (`globalThis.__cockpitDashboardRegistry`)
4. Wire broadcast calls from existing `getBroadcastService().broadcast()` locations

## Files
- `packages/hub-server/src/services/dashboard-registry.ts` (CREATE)
- `packages/hub-server/src/services/index.ts` (MODIFY - export)
- `packages/hub-server/src/api/dashboard-ws.ts` (MODIFY - use registry)
## Acceptance
- [ ] `DashboardRegistry` class with register/unregister/broadcast methods
- [ ] Multiple dashboard clients receive broadcast events
- [ ] Client cleanup on disconnect
- [ ] HMR-safe singleton pattern
- [ ] No TypeScript errors
## Done summary
## Summary

Created `DashboardRegistry` service as a proper HMR-persistent singleton:

**New file:** `packages/hub-server/src/services/dashboard-registry.ts`

**Features:**
- `register(ws)` - Register client, returns `DashboardClient`
- `unregister(clientId)` / `unregisterByWs(ws)` - Remove client
- `broadcast(type, data)` - Send to all clients (typed!)
- `sendToClient(clientId, type, data)` - Send to specific client
- `sendRaw/sendRawToWs` - For responses with `id` field
- HMR-persistent via `globalThis.__cockpitDashboardRegistry`

**Refactored:**
- `dashboard-ws.ts` now uses registry instead of inline Map
- Cleaner separation of concerns
## Evidence
- Commits:
- Tests: bun run typecheck passed
- PRs: