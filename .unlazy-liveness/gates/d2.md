# Gates: D2 — claude bridge, custody, boundary hand-off

Scope: packages/agent/src/harnesses/claude.ts, packages/agent/src/sessiond-client.ts (new),
packages/agent/src/session.ts, their tests. The SDK's `spawnClaudeCodeProcess` gets the
sessiond bridge UNCONDITIONALLY (no flag — operator's cutover order). Custody per design
§4.1: ring replay through the pure `toNeutral`, parked `control_request`s re-parked and
answerable with raw `control_response` by requestId, non-permission control during custody
fails visibly in-band, hand-off at the next `result` line (stdin EOF + respawn with
`resume: sessionId`). Ad-hoc auto-spawn of sessiond ONLY when no service manager owns the
agent (INVOCATION_ID / launchd detection); under a service, a missing sessiond is a loud
install-time error. Adoption spike (second initialize on a live child): timeboxed 90 min,
outcome recorded either way, custody ships regardless.

- [x] G1: spawn path goes through sessiond — no direct child spawn remains in the claude adapter
  CHECK: cd /home/bewinxed/cockpit && grep -c "spawnClaudeCodeProcess" packages/agent/src/harnesses/claude.ts
  EXPECT: /[1-9]/
  EVIDENCE: `grep -c` → `1`, at claude.ts:640 inside the `query()` options, placed AFTER the
  caller's `options` spread so a spawn payload cannot opt out. No flag, no in-process
  fallback (PLAN.md C7). And nothing else in the adapter spawns a child:
  `grep -n "child_process\|Bun.spawn\|spawnSync" packages/agent/src/harnesses/claude.ts` →
  no matches (exit 1).

- [x] G2: agent suite green ≥ 124 with the bridge in place
  CHECK: cd /home/bewinxed/cockpit && bun test packages/agent/ 2>&1 | grep -E "[0-9]+ fail" | tail -1
  EXPECT: 0 fail
  EVIDENCE: `bun test packages/agent/` → ` 144 pass` / ` 0 fail` / `427 expect() calls`,
  19 files (baseline was 131/0 across 17). Whole repo: `bun test` → `587 pass` / `0 fail`
  across 54 files (baseline 553/0). `cd packages/agent && npx tsc --noEmit` → exit 0.
  `npx eslint` clean on all six touched files.

- [x] G3: survival smoke — a scripted fake-CLI child spawned through sessiond keeps running and its ring keeps filling while the supervisor object is torn down and rebuilt; the rebuilt side replays and continues without gap or duplicate
  EVIDENCE: two tests, both driving a REAL sessiond in a separate process on a scratch
  socket with a scripted `node`/`bun` emitter child (never `claude`, never the real
  endpoint).
  `packages/agent/src/sessiond-client.test.ts` — "a child keeps running and its ring keeps
  filling while the agent side is torn down and rebuilt":
    expect(held?.alive).toBe(true);
    expect(held!.head).toBeGreaterThan(cursor);          // ring filled with NOBODY attached
    expect(replayed[0]!.seq).toBe(cursor + 1);           // NO GAP at the seam
    for (…) { expect(replayed[i]!.seq).toBe(replayed[i-1]!.seq + 1);
              expect(replayed[i]!.n).toBe(replayed[i-1]!.n + 1); }   // NO DUPLICATE, no hole
    expect(seen.at(-1)!.n + 1).toBe(replayed[0]!.n);     // the two lives join exactly
  `packages/agent/src/harnesses/claude-custody.test.ts` — "custody survives the owner being
  torn down and rebuilt, and replays without gap or duplicate": the same seam through the
  ADAPTER (ClaudeHarness A adopts, is destroyed, ClaudeHarness B adopts from the cursor),
  asserting the child's own counter continues by exactly one per neutral frame:
    expect(numbers[0]).toBe(lastN + 1);
    for (…) expect(numbers[i]).toBe(numbers[i - 1]! + 1);
  Run: `bun test packages/agent/src/sessiond-client.test.ts` → 5 pass / 0 fail;
  `bun test packages/agent/src/harnesses/claude-custody.test.ts` → 8 pass / 0 fail.

- [x] G4: custody answers a parked permission by requestId with a raw control_response; a non-permission control during custody fails visibly (both tested against recorded CLI fixtures — record the fixture's CLI version in the file header)
  EVIDENCE: fixture `packages/agent/src/harnesses/fixtures/custody-control.ndjson`, header
  records claude CLI **2.1.251** and `@anthropic-ai/claude-agent-sdk` **0.3.220**, with the
  sdk.d.ts line references for every shape (SDKControlRequest 3722-3726,
  SDKControlPermissionRequest 3597-3640, SDKControlResponse 3766-3769) and an explicit
  statement that the lines were captured from a scripted STAND-IN CLI, not a live `claude`
  session (safety boundary — see DEVIATIONS in the leaf report).
  `claude-custody.test.ts` drives `ClaudeCustody` off those lines:
    permission re-parked by the SDK's own id →
      expect(r.rec.permissions).toEqual([{ requestId: 'req-perm-1', toolName: 'Bash',
        input: { command: 'rm -rf build' } }]);
    answered as a RAW control_response →
      expect(JSON.parse(r.written[0]!)).toEqual({ type: 'control_response',
        response: { subtype: 'success', request_id: 'req-perm-1', response: result } });
      expect(r.written[0]!.endsWith('\n')).toBe(true);
    non-permission control (`mcp_message`) fails VISIBLY and IN BAND →
      expect(answer.response.subtype).toBe('error');
      expect(answer.response.request_id).toBe('req-mcp-1');
      expect(frame.subtype).toBe(CUSTODY_DEGRADED);   // and the reader is told, in-transcript
      expect(r.custody.parked).toEqual([]);           // nothing offers a bogus answer for it
    plus the boundary hand-off at the `result` line (stdin EOF once, then respawn-with-resume):
      expect(r.stdinEnds).toBe(1);
      expect(r.handoffs).toEqual([{ instanceId: 'inst-1', sessionId: 'ses-custody-1', held: [] }]);
  8 pass / 0 fail.

- [x] G5: spike outcome recorded: adoption works (and is adopted) OR the boundary hand-off stands, with the observed CLI behavior quoted
  EVIDENCE: **the boundary hand-off stands; adoption is NOT adopted.** The live experiment
  the spike calls for — a second `initialize` on a running child's stdio — could not be run:
  this machine holds ~45 of the operator's real `claude` sessions and the leaf's safety
  boundary forbids spawning or touching one, and a scripted stand-in child cannot answer a
  question about the real CLI's protocol state machine.
  What WAS observed, statically, in the installed CLI bundle
  (`/home/bewinxed/.local/share/claude/versions/2.1.251`, 214 MB, the binary `claude`
  resolves to). The `initialize` branch sits in a plain per-request `if / else if` chain
  over incoming control requests, with NO already-initialized rejection anywhere in it —
  verbatim, from `grep -a -o 'subtype==="initialize"){let A=d.request.sdkMcpServers…'`:

      …else if(d.request.subtype==="initialize"){let A=d.request.sdkMcpServers,
      O=d.request.webSearchIsolationExemptMcpServers; … if(d.request.sdkMcpServers&&
      d.request.sdkMcpServers.length>0){for(let Le of d.request.sdkMcpServers){ … if(tt){
      if(tt.timeout!==Ot)n(`initialize: SDK MCP server '${Le}' is already registered; its
      timeout change is ignored until the server is removed and re-added`);continue} … }
      … let qe=!u; … if(qe&&Prr()&&RQe()==="fresh")kWn(e.host);if(U.hasCommandsInQueue())jr()}

  Two things are quotable there: the CLI anticipates a re-`initialize` (it has a dedicated
  "is already registered" path for MCP servers it has seen before), and it branches on a
  prior-initialization flag (`let qe=!u`) for a one-time side effect rather than refusing.
  That is *suggestive* that a second `initialize` would be accepted — and it is not
  sufficient. It says nothing about the SDK client half (a fresh `Query` initializing
  against a child mid-turn, with queued commands and parked control requests), which is the
  half that would actually have to hold. Betting 45 live sessions on an inference from a
  minified bundle is not a trade this leaf will make.
  Verdict recorded, per the gate's own terms: **custody + boundary hand-off ship**, one
  graceful respawn per session per agent restart, at a turn boundary. Adoption stays
  available as a later, cheap upgrade: it would remove the respawn and change nothing else
  in this leaf's design, and it needs a live spike on a machine with no real sessions on it.
  Time spent: inside the 90-minute box.

- [x] G6: no auto-spawn under service management (unit test on the guard)
  EVIDENCE: `packages/agent/src/sessiond-client.test.ts`, two tests.
  The detector's truth table ("serviceManaged reads systemd, launchd and cockpit's own unit
  marker"):
    expect(serviceManaged({ INVOCATION_ID: 'e6b1c0…' })).toBe(true);   // systemd.exec(5)
    expect(serviceManaged({ COCKPIT_SERVICE_MODE: 'dev' })).toBe(true); // cli/src/service.ts:45
    expect(serviceManaged({ XPC_SERVICE_NAME: 'com.cockpit.agent' })).toBe(true);  // launchd
    expect(serviceManaged({ XPC_SERVICE_NAME: '0' })).toBe(false);      // a login shell is not
    expect(serviceManaged({})).toBe(false);
  And the refusal ("under service management a missing sessiond is a loud error, NEVER an
  ad-hoc spawn"):
    await expect(ensureSessiond(endpoint, { INVOCATION_ID: 'unit-1' }))
      .rejects.toBeInstanceOf(SessiondUnavailableError);
    await expect(ensureSessiond(endpoint, { INVOCATION_ID: 'unit-1' }))
      .rejects.toThrow('systemctl --user start cockpit-sessiond');
    expect(await probeEndpoint(endpoint, 200)).toBe(false);   // nothing was started
  Live corroboration, unplanned and worth recording: the FIRST run of the agent suite after
  the bridge landed failed four pre-existing tests with exactly this error — this session's
  own test process inherits `INVOCATION_ID` from `cockpit-agent.service`, so the guard fired
  against the real endpoint and refused rather than spawning a daemon into the agent's
  cgroup. The guard works on this machine, not only in its own unit test. (Those four tests
  now point at a scratch sessiond of their own.)

## Ledger: 6 of 6 finished. 0 abandoned.
