# Gates: D0 — extraction (mechanical, zero behavior change)

Scope: packages/core/src/ring.ts (SessionRing lifted from packages/hub/src/stream.ts,
hub re-imports), packages/core/src/sessiond.ts (protocol types, capability string
`sessiond.v1`, `sessiondEndpoint()` per design §12), exports from core index.

- [x] G1: SessionRing lives in core, hub consumes it, hub suite green ≥ 84 (behavior identical)
  CHECK: cd /home/bewinxed/cockpit && grep -c "SessionRing" packages/core/src/ring.ts && bun test packages/hub/ 2>&1 | grep -E "[0-9]+ fail" | tail -1
  EXPECT: 0 fail
  EVIDENCE: 2 | 0 fail

- [x] G2: sessiondEndpoint covers linux/darwin/win32 derivations
  CHECK: cd /home/bewinxed/cockpit && grep -c "XDG_RUNTIME_DIR\|\\\\\\\\.\\\\pipe\|.cockpit/sessiond.sock" packages/core/src/sessiond.ts
  EXPECT: /[2-9]/
  EVIDENCE: 7

- [x] G3: all package typechecks exit 0
  CHECK: cd /home/bewinxed/cockpit && bun run typecheck 2>&1 | grep -c "Exited with code 0"
  EXPECT: /^[5-9]/
  EVIDENCE: 6 (actual workspace count is 6 — auth, core, hub, agent, cli, dashboard — not
    the 5 the brief estimated; still matches EXPECT's /^[5-9]/, no change needed. First run
    of the day showed 3/6, because packages/hub/src/db/index.ts — forbidden to this leaf,
    owned by concurrently-running A2 — was mid-edit with a dangling `RESTART_RESUMABLE`
    reference; re-run after A2 landed its fix shows 6/6.)
