# Gates: A1 — wire contract (heartbeat instances, sleeping union, TS2345 fix)

Scope: packages/core/src/index.ts + packages/agent/src/daemon.ts only. Heartbeat payload
carries the live instance list; core status union gains 'sleeping'; the pre-existing
daemon.ts `process.off` TS2345 is fixed so agent typecheck is a usable gate from here on.

- [x] G1: heartbeat payload type + daemon emit carry `instances`
  CHECK: cd /home/bewinxed/whiffle && grep -n "instances" packages/agent/src/daemon.ts | grep -c "heartbeat\|instanceIds" ; grep -c "instances: string\[\]" packages/core/src/index.ts
  EXPECT: /[1-9]/
  EVIDENCE: 3 | 2

- [x] G2: 'sleeping' is in the core instance status union
  CHECK: cd /home/bewinxed/whiffle && grep -c "'sleeping'" packages/core/src/index.ts
  EXPECT: /^[1-9]/
  EVIDENCE: 1

- [x] G3: agent typecheck exits 0 (pre-existing TS2345 fixed, nothing new)
  CHECK: cd /home/bewinxed/whiffle && bun run --filter '@whiffle/agent' typecheck >/dev/null 2>&1 && echo CLEAN
  EXPECT: CLEAN
  EVIDENCE: CLEAN

- [x] G4: agent suite unbroken
  CHECK: cd /home/bewinxed/whiffle && bun test packages/agent/ 2>&1 | grep -E "[0-9]+ fail" | tail -1
  EXPECT: 0 fail
  EVIDENCE: 0 fail

- [x] G5: core + hub + dashboard typechecks still exit 0
  CHECK: cd /home/bewinxed/whiffle && bun run --filter '@whiffle/core' typecheck >/dev/null 2>&1 && bun run --filter '@whiffle/hub' typecheck >/dev/null 2>&1 && bun run --filter '@whiffle/dashboard' typecheck >/dev/null 2>&1 && echo CLEAN
  EXPECT: CLEAN
  EVIDENCE: CLEAN
