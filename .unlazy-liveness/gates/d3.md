# Gates: D3 — the ingest ledger (the no-silent-loss guarantee)

Scope: packages/hub/src/stream.ts, packages/hub/src/server.ts (frames ingest ledger record +
register-ack `ingested`), the agent's frame envelope gaining srcEpoch/srcSeq
(packages/agent/src/session.ts sink), the `sessiond_stream_gap` seam frame, and a property
test. Design §7 invariant: a line reaches the hub as a frame at most once per
(instanceId, epoch, srcSeq); a gap is always an announced reset, never a splice.

- [ ] G1: property test sweeps agent-restart × hub-restart × sessiond-restart × ring-overflow and asserts at-most-once + no-silent-gap on every path
  CHECK: cd /home/bewinxed/cockpit && bun test packages/hub/ packages/agent/ 2>&1 | grep -E "[0-9]+ fail" | tail -2
  EXPECT: 0 fail
  EVIDENCE: pending (quote the property test's case count)

- [ ] G2: honest-loss rule — no ledger entry or stale epoch replays nothing and follows from head (tested)
  EVIDENCE: pending

- [ ] G3: overflow during absence lands a `sessiond_stream_gap` system frame in the stream (tested end-to-end through the hub harness)
  EVIDENCE: pending

- [ ] G4: register ack carries `ingested`; an old-shape ack (absent field) leaves the agent following from head — additive, tested
  EVIDENCE: pending
