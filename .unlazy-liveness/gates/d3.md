# Gates: D3 — the ingest ledger (the no-silent-loss guarantee)

Scope: packages/hub/src/stream.ts, packages/hub/src/server.ts (frames ingest ledger record +
register-ack `ingested`), the agent's frame envelope gaining srcEpoch/srcSeq
(packages/agent/src/session.ts sink), the `sessiond_stream_gap` seam frame, and a property
test. Design §7 invariant: a line reaches the hub as a frame at most once per
(instanceId, epoch, srcSeq); a gap is always an announced reset, never a splice.

- [x] G1: property test sweeps agent-restart × hub-restart × sessiond-restart × ring-overflow and asserts at-most-once + no-silent-gap on every path
  CHECK: cd /home/bewinxed/whiffle && bun test packages/hub/ packages/agent/ 2>&1 | grep -E "[0-9]+ fail" | tail -2
  EXPECT: 0 fail
  EVIDENCE: packages/hub/src/ingest.test.ts sweeps 64 cases — 2 agent-restart counts × 2 hub
  restart × 2 sessiond restart × 2 absence lengths (3 lines, and 20 > the sweep's ring bound
  of 8, i.e. overflow) × 2 live-line counts × 2 re-send tails; asserted at runtime
  (`expect(CASES.length).toBe(64)`). Each case asserts (a) no (epoch,srcSeq) and no child
  counter ingested twice, (b) consecutive ingested lines contiguous within one hub life and
  one sessiond epoch unless a `sessiond_stream_gap` frame, an epoch change or a hub restart
  announced the discontinuity. The sweep runs production code: `createStreamHub`'s ledger,
  the real `SessionRing`, and `resumeCursor`/`alreadyIngested` from @whiffle/core.
  `bun test packages/hub/ packages/agent/` -> "166 pass 0 fail" and "162 pass 0 fail";
  whole repo `bun test` -> "688 pass, 0 fail, 2748 expect() calls, 59 files".
  Mutation-checked: deleting the ledger's dedupe line turns 66 of 71 sweep tests red.

- [x] G2: honest-loss rule — no ledger entry or stale epoch replays nothing and follows from head (tested)
  EVIDENCE: `resumeCursor(epoch, mark)` (packages/core/src/stream.ts) returns undefined unless
  the mark's epoch is the epoch sessiond is running NOW; `SessionSupervisor.reattach` passes
  that (or nothing) to `adopt`. Unit: ingest.test.ts "no ledger entry means replay nothing and
  follow from head" (a hub that never ingested offers `{}`, not a zero mark) and "a mark from a
  dead sessiond epoch replays nothing". End-to-end against a real sessiond in a separate
  process: packages/agent/src/reattach-ledger.test.ts "a stale-epoch mark replays nothing and
  follows from head" — 6 lines already in the ring, zero replayed, then the next two lines
  (n=7,8) arrive live; and the converse "a usable mark replays exactly the gap the hub named,
  in order" — mark srcSeq 4 of 9 lines replays exactly [5,6,7,8,9].

- [x] G3: overflow during absence lands a `sessiond_stream_gap` system frame in the stream (tested end-to-end through the hub harness)
  EVIDENCE: agent half — reattach-ledger.test.ts "a ring that overflowed during the absence
  lands a sessiond_stream_gap frame, not a splice": a real sessiond, a scripted child pushed
  past the real 4096-line ring bound (waited on the daemon's own head, > 4200), a reattach at
  mark srcSeq 2, and the supervisor's sink receives a `system`/`sessiond_stream_gap` frame
  whose text matches /resumes at line \d+/, with zero pre-seam lines replayed. Hub half —
  ingest.test.ts "the announced seam is relayed to a following dashboard like any other
  frame": a real `createServer`, a real agent socket, a real `stream.subscribe`; the dashboard
  receives seq 2 = the `sessiond_stream_gap` frame between srcSeq 1 and srcSeq 4321.

- [x] G4: register ack carries `ingested`; an old-shape ack (absent field) leaves the agent following from head — additive, tested
  EVIDENCE: hub — `registerAck` in packages/hub/src/server.ts sends `{ok:true, ingested}` off
  `streams.ingestedFor(peekInstances(payload))`. Wired test "the register ack carries the
  ledger, and a replayed line is ingested once" (real createServer + real sockets): first ack
  `ingested: {}`, then after frames at srcSeq 1..3 the next ack carries
  `{epoch, srcSeq: 3}`, and a replay of srcSeq 2,3,4 sequences ONE frame (hub seq 4 = "replay
  4"); the poll for a seq 5 times out, i.e. the duplicates never became frames.
  Agent — `readIngested` returns undefined for an old-shape payload, and
  reattach-ledger.test.ts "an ack with no ingested field is tolerated: the agent follows from
  head" drives `supervisor.reattachFrom({ ok: true }, rows)` against a real sessiond: nothing
  replayed, the next live line arrives.
