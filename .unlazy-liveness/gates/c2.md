# Gates: C2 — convergence surfaced on the board

Scope: dashboard only (client.svelte.ts readers + the machine-card component the opening
grep identifies). Renders per machine: commits-behind-hubBuild (from A2's `hubBuild` +
agents' `build`), fleet-sync failures (e.g. memory `failed: edited on this machine`) as a
first-class badge with the existing adopt/overwrite affordance linked, and update-pending
state. No new colors or tokens outside app.css.

- [x] G1: behind/failed/pending render from frame data alone (unit tests over the derivations)
  CHECK: cd /home/bewinxed/whiffle && bun test apps/dashboard/src/lib/whiffle/ 2>&1 | grep -E "[0-9]+ fail" | tail -1
  EXPECT: 0 fail
  EVIDENCE: ` 282 pass` / ` 0 fail` (baseline 270 + 12 new in `convergence.test.ts`, no other
  whiffle test file touched). Whole-repo `bun test packages/ apps/` → `706 pass / 0 fail`
  (baseline 689). New module `apps/dashboard/src/lib/whiffle/convergence.ts` holds every
  reader, all pure and covered:
  - `buildConvergence(build, hubBuild)` → `'unknown' | 'current' | 'behind'`. Tested: a
    machine with `build.commit` undefined reads `unknown` even against a real `hubBuild`
    (the Mac's exact incident — `commit: None` must never read as current); a `hubBuild`
    that is itself absent (a hub older than A2) is *also* `unknown`, never `current`, so
    "nothing to compare against" can't masquerade as convergence; matching commits →
    `current`, differing → `behind`.
  - `fleetSyncFailures(fleet)` scans every `FleetItemState`-keyed category
    (mcp/marketplaces/plugins/skills/memoryDocs/hooks) plus the singular
    memory/memoryHook rows, returns only rows at `state: 'failed'` with their `detail`
    verbatim — `applied`/`removed` rows are left out (tested). The hub's own copy for the
    incident (`edited on this machine — adopt it or overwrite`) round-trips unmodified.
  - `fleetSyncAgeMs(fleet, now)` — ms since `fleet.at`; `undefined` for a machine that has
    never synced (tested against an 11-day gap, the exact number named in the brief).
  - `deployInfoOf(deploy)` / `isDeployDiverged` / `isDeployNoteworthy` — structural reader
    over C1's `DeployTick.state.kind` vocabulary (mirrored as a local `DeployKind` union
    since packages/agent is out of scope and C1's own G3 says the wiring onto the hub
    frame is left to "leaf C2 subscribes there" — not yet landed by any leaf). Reads
    `undefined` off anything not shaped like `{kind: string}` — the same "hub predates
    this field" shape `pulses`/`hubBuild` already use in client.svelte.ts. `unmarked` and
    `current` are not noteworthy (an ordinary dev tree is not a fact to flag); every other
    kind is, and `diverged` is flagged apart as the one that must read as a refusal, not a
    stale badge.
  `client.svelte.ts` gained `state.hubBuild` (folded from `frame.hubBuild` on the
  `instances` frame with the exact same structural cast `pulses`/`handoffs` already use —
  additive field, absent from a hub that predates it) and a `whiffle.hubBuild` getter.

- [x] G2: dashboard typecheck exits 0
  CHECK: cd /home/bewinxed/whiffle && bun run --filter '@whiffle/dashboard' typecheck >/dev/null 2>&1 && echo CLEAN
  EXPECT: CLEAN
  EVIDENCE: `CLEAN`. Full output shows only 6 pre-existing `state_referenced_locally`
  warnings in `src/routes/session/+layout.svelte` and `Shell.svelte` — neither touched by
  this leaf — and 0 errors. `npx eslint` on every touched file
  (`convergence.ts`, `convergence.test.ts`, `MachineCard.svelte`, `client.svelte.ts`,
  `FleetBoard.svelte`, `apps/dashboard/src/routes/tools/+page.svelte`) prints nothing.

- [x] G3: RENDERED-LAYOUT GATE — ui-observer, same discipline as A3-G5, on a machine card showing a behind-badge and a failed-sync badge
  EVIDENCE: Measured against the live hub at **http://localhost:3000/session** (localhost,
  not the tailnet IP), via claude-in-chrome, injecting `~/.claude/skills/ui-observer/observer.browser.js`
  through a `<script src>` served from a local `python3 -m http.server 8931` scratch dir
  (cross-origin `fetch` of the file is blocked, the tag load is not) and calling
  `window.__uiObserver({scopeSel: '.machine-list'})`.

  The running hub is pinned pre-A2/pre-C1 (confirmed: every machine's `build` arrives as
  `{version, startedAt}` with no `commit`, and there is no `deploy` field at all), so
  `hubBuild` was driven into the *live* store rather than the hub restarted (forbidden) —
  found via `performance.getEntriesByType('resource')` for the app's own HMR-versioned
  `client.svelte.ts` URL and `import()`-ed *that exact URL* (confirmed the same instance:
  `whiffle.hub === 'connected'` and `whiffle.machines.length === 3` matched the real
  rendered sidebar count before and after). One real bug surfaced and fixed by this probe:
  `MachineCard`'s `Tooltip.Root` threw `Context "Tooltip.Provider" not found` inside
  `FleetBoard.svelte` (which — unlike `tools/+page.svelte` — had never needed a
  `Tooltip.Provider` ancestor before), silently aborting the whole board's mount and
  showing a stale empty page; fixed by wrapping the new machines panel in its own
  `<Tooltip.Provider>`, scoped there since nothing else on the board uses one.

  With the fix live (page reloaded), on `Omars-MacBook-Pro` (real hostname from the live
  fleet) `build.commit` was set to `'old1234'` and `whiffle.hubBuild` to
  `{commit: 'a56a42c', ...}` (the real hub's own sha named in the brief) to exercise
  `behind`, and `fleet.memory` to `{state:'failed', detail:'edited on this machine — adopt
  it or overwrite'}` — the hub's own copy — to exercise the sync-failure badge:

  DOM readback right after injecting, before anything else touched the store:
  `["obelisk-of-light Build unknown","Omars-MacBook-Pro Behind hub Fleet sync failed","box Build unknown"]`
  — both badges present together on the one card, exactly what this gate asks for.
  Screenshot at that instant confirms it visually: obelisk-of-light and box each carry a
  single muted outline "Build unknown" pill, Omars-MacBook-Pro carries two warning-tinted
  pills, "Behind hub" and "Fleet sync failed", token-styled off `--warning-3`/`--warning-11`
  (Sidebar.svelte's A5 pill recipe, no new tokens).

  ui-observer, run in that same window (scope receipt and content stats, quoted verbatim):
  `"scope":{"selector":".machine-list","matchCount":1,"usedElement":"...ul.machine-list..."}`
  `"contentStats":{"visibleElements":41,"textChars":95,"interactiveElements":4,"images":7,"scopeRect":{"w":1504,"h":140}}`
  `"observations":[{"type":"text-truncated","selector":"...li.row:nth-of-type(2) > span.who > span.nm","axis":"horizontal","via":"text-overflow:ellipsis","scrollWidth":114,"clientWidth":112,"hiddenPx":2,"text":"Omars-MacBook-Pro", ...}]`
  — one observation total, `hiddenPx: 2` on the hostname's own intentional
  `max-width:14ch` truncate-with-ellipsis (the tool's own note: "declares an intentional
  truncation affordance… not an overflow defect"), not a layout defect; `containers: []`
  (no sibling-gap or alignment defects found across the three rows).

  Introduced vs pre-existing: the entire `.machine-list` scope is new (leaf C2's own
  markup) — every element and the one truncation observation in it is this leaf's, not a
  pre-existing condition being re-reported.

  Separately, a few seconds later the real hub pushed a fresh `instances` frame (the live
  fleet reconnecting, not anything this leaf did) that replaced `state.machines` wholesale,
  wiping the synthetic `build`/`fleet` mutation on the array — and the card re-rendered
  showing the **genuine** live data: `Omars-MacBook-Pro`'s real `fleet.memory` is in fact
  `{state:'failed', detail:'edited on this machine — adopt it or overwrite'}` (the literal
  incident from the brief, still unresolved on the real fleet right now) and its real
  `build` is `{version:'0.1.0', startedAt:1786216243329}` — no `commit` at all, so the card
  correctly fell back to "Build unknown" rather than claiming current. Clicking the live
  "Fleet sync failed" badge navigated to `/tools#fleet-memory` and landed on the real
  Memory panel (`location.href` → `.../tools#fleet-memory`, `document.getElementById('fleet-memory')`
  truthy) — the adopt/overwrite affordance already there (FleetMemory.svelte's existing
  `driftedRow` snippet), not re-created at the badge. No adopt/overwrite endpoint was
  called at any point.

  Scratch server (`python3 -m http.server 8931` in a `/tmp` dir) stopped and the dir
  removed after measurement; the injected browser-session mutation was never persisted
  anywhere (in-memory only, and the live hub's own next frame overwrote it, as shown
  above). No systemd service touched or restarted.
