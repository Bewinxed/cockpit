# Gates: C2 — convergence surfaced on the board

Scope: dashboard only (client.svelte.ts readers + the machine-card component the opening
grep identifies). Renders per machine: commits-behind-hubBuild (from A2's `hubBuild` +
agents' `build`), fleet-sync failures (e.g. memory `failed: edited on this machine`) as a
first-class badge with the existing adopt/overwrite affordance linked, and update-pending
state. No new colors or tokens outside app.css.

- [ ] G1: behind/failed/pending render from frame data alone (unit tests over the derivations)
  CHECK: cd /home/bewinxed/cockpit && bun test apps/dashboard/src/lib/cockpit/ 2>&1 | grep -E "[0-9]+ fail" | tail -1
  EXPECT: 0 fail
  EVIDENCE: pending

- [ ] G2: dashboard typecheck exits 0
  CHECK: cd /home/bewinxed/cockpit && bun run --filter '@cockpit/dashboard' typecheck >/dev/null 2>&1 && echo CLEAN
  EXPECT: CLEAN
  EVIDENCE: pending

- [ ] G3: RENDERED-LAYOUT GATE — ui-observer, same discipline as A3-G5, on a machine card showing a behind-badge and a failed-sync badge
  EVIDENCE: pending
