# Gates: D1 — the sessiond binary

Scope: packages/sessiond/** (new workspace package). Design doc §3.4/§5/§6/§8/§9/§11 §12:
spawn/write/signal/stdin_end/subscribe/list + welcome{epoch,procs}, per-child ring
(4096 lines / 8 MiB, drop-oldest + explicit reset), commandId dedup (5 min TTL), drain
(stdin-EOF then grace then SIGKILL, drain as a plain function), unix socket 0600 in 0700
dir, stale-socket probe-then-unlink, unknown message types answered
`unsupported`, never fatal. NO parsing of child payloads anywhere.

- [x] G1: all verbs round-trip against real child processes in tests (spawn a script child; write reaches stdin; lines come back with monotonic seq; signal/stdin_end behave)
  CHECK: cd /home/bewinxed/cockpit && bun test packages/sessiond/ 2>&1 | grep -E "[0-9]+ (pass|fail)" | tail -2
  EXPECT: 0 fail
  EVIDENCE: 10 pass | 0 fail

- [x] G2: ring overflow answers a stale cursor with reset, never a spliced stream (test overflows past 4096 and resubscribes)
  CHECK: cd /home/bewinxed/cockpit && bun test packages/sessiond/ -t "G2" 2>&1 | grep -E "[0-9]+ (pass|fail)" | tail -2
  EXPECT: 0 fail
  EVIDENCE: 1 pass | 0 fail

- [x] G3: re-delivered commandId is re-acked, not re-executed (spawn dedup test: child count stays 1)
  CHECK: cd /home/bewinxed/cockpit && bun test packages/sessiond/ -t "G3" 2>&1 | grep -E "[0-9]+ (pass|fail)" | tail -2
  EXPECT: 0 fail
  EVIDENCE: 1 pass | 0 fail

- [x] G4: children survive an agent-side socket drop; a dropped subscriber's backlog replays gap-free from afterSeq (the tmux property, tested in-process)
  CHECK: cd /home/bewinxed/cockpit && bun test packages/sessiond/ -t "G4" 2>&1 | grep -E "[0-9]+ (pass|fail)" | tail -2
  EXPECT: 0 fail
  EVIDENCE: 1 pass | 0 fail

- [x] G5: unknown type → `unsupported` ack, connection stays open
  CHECK: cd /home/bewinxed/cockpit && bun test packages/sessiond/ -t "G5" 2>&1 | grep -E "[0-9]+ (pass|fail)" | tail -2
  EXPECT: 0 fail
  EVIDENCE: 1 pass | 0 fail

- [x] G6: no payload interpretation: sessiond sources contain no JSON.parse of child output beyond newline framing
  CHECK: cd /home/bewinxed/cockpit && grep -rn "JSON.parse" packages/sessiond/src | grep -v "protocol\|socket\|test" | wc -l
  EXPECT: /^0$/m
  EVIDENCE: 0
