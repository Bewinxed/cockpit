# Gates: A2 — hub truth engine

Scope: packages/hub/src/server.ts, packages/hub/src/db/index.ts, packages/hub/src/db/schema.ts,
packages/hub/src/truth.test.ts (new). Contracts C2–C6 + `hubBuild` added to instancesFrame.
No drizzle migration files (text column; TS-side union only).

- [ ] G1: settle rewrite — resumable orphans become `sleeping`, RESTART_RESUMABLE is no longer written
  CHECK: cd /home/bewinxed/cockpit && grep -c "RESTART_RESUMABLE" packages/hub/src/db/index.ts
  EXPECT: /^0$|^1$/
  EVIDENCE: pending  (1 permitted only in the boot-sweep migration read; quote the line)

- [ ] G2: heartbeat reconciliation exists and is tested — a beat listing X promotes X to running; a beat omitting a running row settles it (sleeping when sessionId, error+RESTART_LOST otherwise); no respawn from the heartbeat path
  CHECK: cd /home/bewinxed/cockpit && bun test packages/hub/src/truth.test.ts 2>&1 | grep -E "[0-9]+ (pass|fail)" | tail -2
  EXPECT: 0 fail
  EVIDENCE: pending

- [ ] G3: bounded restore — horizon 30 min / cap 20 newest-first, remainder sleeping, restore writes `starting` not `running`; covered by tests including the "openInstance no longer lies" case
  CHECK: cd /home/bewinxed/cockpit && grep -n "RESTORE_HORIZON_MS\|RESTORE_MAX" packages/hub/src/server.ts packages/hub/src/db/index.ts | head -4
  EXPECT: /RESTORE_HORIZON_MS/
  EVIDENCE: pending

- [ ] G4: boot sweep — running/starting→unknown and legacy error+RESTART_RESUMABLE→sleeping, idempotent (test runs it twice, same result)
  EVIDENCE: pending (quote the test name + pass line)

- [ ] G5: pulse retention + read overlay — instancesFrame carries `pulses` and `hubBuild`; /api/instances and the frame serve `unknown` for rows on machines absent from the registry (test drops a socket and reads both)
  CHECK: cd /home/bewinxed/cockpit && grep -c "pulses\|withSessionPresence\|hubBuild" packages/hub/src/server.ts
  EXPECT: /[3-9]/
  EVIDENCE: pending

- [ ] G6: hub suite green, count ≥ 84
  CHECK: cd /home/bewinxed/cockpit && bun test packages/hub/ 2>&1 | grep -E "[0-9]+ fail" | tail -1
  EXPECT: 0 fail
  EVIDENCE: pending

- [ ] G7: hub typecheck exits 0; no files outside owned set changed
  CHECK: cd /home/bewinxed/cockpit && bun run --filter '@cockpit/hub' typecheck >/dev/null 2>&1 && git status --porcelain -- packages/hub | grep -v "server.ts\|db/index.ts\|db/schema.ts\|truth.test.ts" | wc -l
  EXPECT: /^0$/
  EVIDENCE: pending
