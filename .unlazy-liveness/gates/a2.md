# Gates: A2 — hub truth engine

Scope: packages/hub/src/server.ts, packages/hub/src/db/index.ts, packages/hub/src/db/schema.ts,
packages/hub/src/truth.test.ts (new). Contracts C2–C6 + `hubBuild` added to instancesFrame.
No drizzle migration files (text column; TS-side union only).

- [x] G1: settle rewrite — resumable orphans become `sleeping`, RESTART_RESUMABLE is no longer written
  CHECK: cd /home/bewinxed/whiffle && grep -c "RESTART_RESUMABLE" packages/hub/src/db/index.ts
  EXPECT: /^0$|^1$/
  EVIDENCE: `0`. Zero, not one: `settleInstances` now writes
  `{ status: 'sleeping', lastError: null }` for a resumable orphan (and
  `error` + `RESTART_LOST` for one with nothing to resume), and the boot sweep
  takes the legacy marker as a *parameter* — `sweepBootStatuses(legacyResumableError)` —
  so db/index.ts holds no opinion about what a legacy error said. server.ts
  passes core's constant: `const swept = db.sweepBootStatuses(RESTART_RESUMABLE);`
  Test `the boot sweep reclassifies the states the old taxonomy left behind, twice over`
  asserts `sweep-legacy` moves error+RESTART_RESUMABLE → `sleeping` with `lastError` null.

- [x] G2: heartbeat reconciliation exists and is tested — a beat listing X promotes X to running; a beat omitting a running row settles it (sleeping when sessionId, error+RESTART_LOST otherwise); no respawn from the heartbeat path
  CHECK: cd /home/bewinxed/whiffle && bun test packages/hub/src/truth.test.ts 2>&1 | grep -E "[0-9]+ (pass|fail)" | tail -2
  EXPECT: 0 fail
  EVIDENCE: ` 7 pass` / ` 0 fail` (Ran 7 tests across 1 file. [313.00ms]).
  `db.reconcileHeartbeat(machineId, liveIds, graceMs)` is called from `case 'heartbeat'`
  in server.ts and returns `{ promoted, settled }`. Test
  `a heartbeat listing a session promotes it to running; one that omits it settles it`
  drives a real `/ws` socket: after a beat listing three ids all three read `running`;
  after a beat listing one, `inst-resumable` → `sleeping` with `lastError` null,
  `inst-lost` (no sessionId) → `error` with `lastError === RESTART_LOST`, and
  `expect(spawnsIn(agent).length).toBe(spawnsBefore)` proves no respawn came out of
  that path. A re-listed row is promoted back to `running`. Companion test
  `a freshly-issued spawn is not settled by a beat that was already in flight`
  fences the one race: a `starting` row younger than HEARTBEAT_SETTLE_GRACE_MS
  (45s = 3 beats, our choice) survives an omitting beat and settles after backdating.

- [x] G3: bounded restore — horizon 30 min / cap 20 newest-first, remainder sleeping, restore writes `starting` not `running`; covered by tests including the "openInstance no longer lies" case
  CHECK: cd /home/bewinxed/whiffle && grep -n "RESTORE_HORIZON_MS\|RESTORE_MAX" packages/hub/src/server.ts packages/hub/src/db/index.ts | head -4
  EXPECT: /RESTORE_HORIZON_MS/
  EVIDENCE: packages/hub/src/server.ts:951:   * (see {@link RESTORE_HORIZON_MS} / {@link RESTORE_MAX}) — this function does | packages/hub/src/server.ts:2969:            // {@link RESTORE_HORIZON_MS}, and never m
  `packages/hub/src/server.ts:95:const RESTORE_HORIZON_MS = 30 * 60_000;`
  `packages/hub/src/server.ts:106:const RESTORE_MAX = 20;`
  (both labelled "ours, because…" in prose above the declaration — the horizon covers
  restart cycles measured in seconds-to-minutes, the cap keeps one register from
  stampeding a machine). Register sorts orphans by pre-settle `updatedAt` desc, filters
  on the horizon, `.slice(0, RESTORE_MAX)`. `openInstance` writes `status: 'starting'`
  on both the insert and the conflict-update, so `restore()` can no longer mint a
  `running` row. Tests: `a spawn writes 'starting', not 'running' — openInstance no
  longer lies` and `a register restores the newest orphans only, within the horizon and
  under the cap` (22 recent + 3 two-hour-old resumable orphans, register with
  `instances: []` → exactly 20 spawn envelopes received on the agent socket, the newest
  20, no stale ones; the 20 read `starting`, the other 5 read `sleeping` with
  `lastError` null). Server log from that run:
  `[hub] machine-truth: restoring 20 of 25 orphaned session(s); the rest are sleeping`.

- [x] G4: boot sweep — running/starting→unknown and legacy error+RESTART_RESUMABLE→sleeping, idempotent (test runs it twice, same result)
  EVIDENCE: test `the boot sweep reclassifies the states the old taxonomy left behind,
  twice over` — first call returns `{ toUnknown: 2, toSleeping: 1 }` and second returns
  `{ toUnknown: 0, toSleeping: 0 }` with every status unchanged between the two;
  `stopped` and a genuine `error` ("the harness exited 1") are left alone by both runs.
  Part of the ` 7 pass` / ` 0 fail` line above. Wired at boot in `createServer`, one line
  below `db.markAllAgentsOffline()`. The sweep deliberately does NOT touch `updatedAt`:
  stamping it would make every row on a returning machine look freshly alive and defeat
  the restore horizon.

- [x] G5: pulse retention + read overlay — instancesFrame carries `pulses` and `hubBuild`; /api/instances and the frame serve `unknown` for rows on machines absent from the registry (test drops a socket and reads both)
  CHECK: cd /home/bewinxed/whiffle && grep -c "pulses\|withSessionPresence\|hubBuild" packages/hub/src/server.ts
  EXPECT: /[3-9]/
  EVIDENCE: `13`. `withSessionPresence` overlays both emission points
  (`.get('/api/instances', () => withSessionPresence(db.listInstances()))` and
  `instances: withSessionPresence(db.listInstances())` inside `instancesFrame`);
  the frame payload gained `pulses: Object.fromEntries(pulses)` and
  `...(hubBuild ? { hubBuild } : {})`. Tests
  `a session on a machine the hub cannot reach reads unknown, and the column keeps history`
  (route: `running` while the socket is held, `unknown` after `registry.sockets.clear()`,
  stored column still `running`, and a `stopped` row stays `stopped`) and
  `the instances frame carries the same derived status, plus pulses and hubBuild`
  (opening frame carries the exact pulse object, `hubBuild.version` is a string, the
  frame reads `unknown` after the drop in the same breath as the route, and the pulse is
  gone from the next frame once the beat settles the row).

- [x] G6: hub suite green, count ≥ 84
  CHECK: cd /home/bewinxed/whiffle && bun test packages/hub/ 2>&1 | grep -E "[0-9]+ fail" | tail -1
  EXPECT: 0 fail
  EVIDENCE: ` 94 pass` / ` 0 fail` / `291 expect() calls` / `Ran 94 tests across 9 files.
  [745.00ms]` — 87 before this leaf, +7 from truth.test.ts = 94.

- [x] G7: hub typecheck exits 0; no files outside owned set changed
  CHECK: cd /home/bewinxed/whiffle && bun run --filter '@whiffle/hub' typecheck >/dev/null 2>&1 && git status --porcelain -- packages/hub | grep -v "server.ts\|db/index.ts\|db/schema.ts\|truth.test.ts" | wc -l
  EXPECT: /^0$/m
  EVIDENCE: `0`. `bun run --filter '@whiffle/hub' typecheck` → `@whiffle/hub typecheck:
  Exited with code 0`. `npx eslint` over all four owned files exits clean (the two
  `no-case-declarations` errors the new `case 'register'`/`case 'heartbeat'` bodies first
  raised are fixed by braced case blocks).
