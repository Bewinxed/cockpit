# Gates: A5 — the new statuses reach a fleet surface (found by the parent at A3/G5)

A3 landed the taxonomy correctly in the store and in LiveSessionRow, and the parent's
ui-observer run confirmed sleeping/unknown render on /project/[id] (liveIn -> isListed).
But every fleet-level surface reads `cockpit.runningInstances`, which filters `isLive`
(running|starting) — so a sleeping or unknown session is absent from the board, the
sidebar and the jump palette. The getters that would show them (listedInstances,
staleInstances, notRunningOn, scratchInstances) have zero component consumers.
That makes `unknown` invisible exactly where the operator looks first, which is the
defect this build exists to remove.

- [x] G1: at least one fleet-level surface lists non-live rows, distinctly from live work
  CHECK: grep -rl "listedInstances\|staleInstances\|notRunningOn" apps/dashboard/src --include=*.svelte | wc -l
  EXPECT: /^[1-9]/m
  EVIDENCE: 2

- [x] G2: dashboard suite green and typecheck clean
  CHECK: cd /home/bewinxed/cockpit && bun test apps/dashboard/src/lib/cockpit/ 2>&1 | grep -E "[0-9]+ fail" | tail -1 && bun run --filter '@cockpit/dashboard' typecheck >/dev/null 2>&1 && echo CLEAN
  EXPECT: /0 fail[\s\S]*CLEAN/
  EVIDENCE: 0 fail | CLEAN

- [x] G3: RENDERED-LAYOUT GATE — ui-observer on the fleet surface with a sleeping and an
      unknown row present; quote scope receipt, contentStats and observations.
  EVIDENCE: http://localhost:3000/session, real hub state (176 unknown + 1 error) plus one
    injected synthetic sleeping row. FleetBoard `.not-running`: scope {matchCount:1,
    usedElement:"...div.not-running:nth-of-type(4)"}, contentStats {visibleElements:2584,
    textChars:18875, interactiveElements:177, images:177, scopeRect:{w:1504,h:6419.3}} — 1
    observation (sibling-padding-differs: heading declares its own 16px inset, the bare
    flex-column rows wrapper declares none because each row supplies its own via
    LiveSessionRow's px-4 — measured aligned: secTextLeft 370 === rowMarkLeft 370). Scoped
    to `.not-running-rows` alone (the 177 rows): 0 observations, contentStats
    {visibleElements:2582, textChars:18857, interactiveElements:177, images:177,
    scopeRect:{w:1482,h:6372}}. Sidebar `.rail` "Not running" rows block: scope
    {matchCount:1, usedElement:"...div.rows:nth-of-type(7)"}, contentStats
    {visibleElements:1369, textChars:13171, interactiveElements:177, images:177,
    scopeRect:{w:307,h:5486}} — observation counts {sibling-padding-differs:177,
    sibling-gaps-differ:18, text-truncated:169, near-miss-alignment:3 (deltaPx 0.6,
    sub-pixel)}, zero of small-tap-target/escapes-viewport/horizontal-overflow/
    text-clipped/fill-bleeds-past-inset. text-truncated is the declared ellipsis
    affordance on long titles (by the scanner's own definition, not a defect);
    sibling-padding-differs is the pre-existing `.nm`/Badge row structure shared with
    "Running now"/project sub-lists. Fixed one real finding along the way: FleetBoard's
    "Not running" heading originally used --space-3 (11px) while LiveSessionRow's own
    inset is Tailwind's plain px-4 (16px) — re-measured after the fix, 0px delta.
