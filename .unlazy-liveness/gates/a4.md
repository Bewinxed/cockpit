# Gates: A4 — the split doctrine filed

Scope: ARCHITECTURE.md (new, repo root) carrying the PLAN's "operator's question, answered"
section expanded: the three roles, the derived-liveness law, the status taxonomy with
meanings, who writes what, and the sessiond boundary (owns process/pipe/buffer, never
meaning). Nothing else changes.

- [x] G1: file exists, was not previously present, and names the law verbatim
  CHECK: cd /home/bewinxed/whiffle && git log --oneline -1 -- ARCHITECTURE.md >/dev/null 2>&1; grep -c "never serve stored liveness" ARCHITECTURE.md
  EXPECT: /^[1-9]/
  EVIDENCE: 1

- [x] G2: taxonomy table present with all seven statuses
  CHECK: cd /home/bewinxed/whiffle && grep -c "sleeping\|unknown\|discarded" ARCHITECTURE.md
  EXPECT: /[3-9]/
  EVIDENCE: 13
