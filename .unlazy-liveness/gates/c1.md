# Gates: C1 — the deployment clone and the poller

Scope: packages/agent/src/deploy.ts (new), packages/agent/src/update.ts (trigger
integration), packages/cli/src/service.ts + a `whiffle deploy init` verb (service.ts is
yours now — B2 and D4 have landed). Contract C8: clone at ~/.whiffle/app tracking
origin/main, `.whiffle-deploy` marker, 60 s fetch-and-compare poller that ONLY acts in a
marked checkout, update flow unchanged (pull --ff-only, install, build, restart). A dev
tree must be structurally unable to auto-pull.

- [x] G1: deploy init builds a working clone layout in a scratch dir (test uses a local bare repo as origin: clone, marker, units generated pointing at the clone with the C9 DB path)
  CHECK: cd /home/bewinxed/whiffle && bun test packages/cli/ packages/agent/ 2>&1 | grep -E "[0-9]+ fail" | tail -2
  EXPECT: 0 fail
  EVIDENCE: `bun test packages/cli/ packages/agent/` → `187 pass / 0 fail / 566 expect() calls,
  22 files` (cli 21→32, agent 131→142; whole repo `bun test` → 609 pass / 0 fail / 56 files).
  packages/cli/src/deploy-init.test.ts drives `deployInit` against a bare repo made in a
  mkdtemp scratch (`git init --bare --initial-branch=main`), cloning into
  `<scratch>/home/.whiffle/app`; `bun install` and the dashboard build are stubbed through the
  injected StepRunner, the unit install through the injected installer, so nothing shells out
  to systemctl/launchctl (asserted: "nothing it ran was a service command"). Asserted layout:
  `git rev-parse --abbrev-ref HEAD` = `main`; `remote get-url origin` = the bare repo;
  `.whiffle-deploy` parses and matches `{root, origin, branch: 'main', createdBy: 'whiffle
  deploy init', createdAt}`; `git status --porcelain` in the clone is EMPTY (the marker is
  written into `.git/info/exclude`, or the update flow's dirty guard would lock the machine
  out of its own deploys forever); install precedes build, both inside the clone.
  Units generated for root `~/.whiffle/app`, quoted from `serviceDefinition(id,'prod','systemd',root)`:
    hub       ExecStart=…/bun /home/bewinxed/.whiffle/app/packages/hub/src/index.ts
              WorkingDirectory=/home/bewinxed/.whiffle/app
              Environment=WHIFFLE_DB_PATH=/home/bewinxed/.local/share/whiffle/whiffle.db   ← C9
    dashboard ExecStart=…/bun /home/bewinxed/.whiffle/app/apps/dashboard/build/index.js
    sessiond  ExecStart=…/bun /home/bewinxed/.whiffle/app/packages/sessiond/src/main.ts
    agent     ExecStart=…/bun /home/bewinxed/.whiffle/app/packages/cli/src/cli.ts up
  The DB path assertion is two-sided: it equals the platform data dir AND does not start with
  the clone root. `~/.whiffle/app` was NOT created on this machine (`ls -d ~/.whiffle` →
  "No such file or directory"); the three real units are untouched (mtimes 2026-08-09/14).

- [x] G2: poller acts only behind the marker — unmarked checkout: fetch/compare never runs (tested); marked + behind → update flow invoked exactly once per new head (tested with the scratch origin advancing)
  EVIDENCE: packages/agent/src/deploy.test.ts, 11 pass / 0 fail.
  Unmarked half — `checkDeploy` reads the marker BEFORE any git runs, so with a spy GitRunner
  that records every call and fails loudly if reached: "in an unmarked checkout, no git command
  runs at all — not even the fetch" asserts `state.kind === 'unmarked'` and `calls === []`;
  the poller test ticks a DeployWatcher twice over the unmarked clone and asserts `runs === []`
  and `calls === []`. A malformed or partial marker is treated as no marker (fails closed) —
  also asserted with zero git calls.
  Marked half — a real clone of the scratch bare origin with a marker: tick 1 on a level clone
  → `current`, 0 updates. `commit('second')` + push → tick → `behind`, `updated: true`, 1 run,
  and the clone's HEAD now equals origin/main. Four more ticks on the same head → still 1 run,
  state back to `current`. `commit('third')` → tick → 2nd run, with a different target sha.
  Dedup is by attempted target, so an update that THROWS is also not retried on the same head
  (asserted: 3 ticks, `attempts === 1`, failure surfaced through the report callback).
  No timer runs anywhere in the tests: `DeployWatcher.tick()` is called explicitly;
  `startDeployPoller` (setInterval, unref'd) is never invoked by a test.

- [x] G3: the poller's pull is --ff-only against pinned origin/main; a diverged clone refuses loudly and surfaces on the board rather than resetting
  EVIDENCE: the refusal path is `deploy.test.ts › a diverged clone refuses loudly (G3)`.
  Pin — `pullArgs` is exported from update.ts and asserted directly:
    `pullArgs('main')` → `['pull','--ff-only','origin','main']`  (what the poller always passes)
    `pullArgs()`       → `['pull','--ff-only']`                   (the unchanged manual control)
    both contain `--ff-only` and neither contains `--rebase` or `--force`.
  Refusal — "divergence is detected, named, and never resolved by resetting": a marked clone
  gets a local commit ("a fix somebody made on the box"), origin then advances. One tick gives
  `kind: 'diverged'`, `ahead: 1`, `behind > 0`, `updated: false`, and the update flow is
  invoked ZERO times. `describeDeploy` is asserted to contain "DIVERGED", "Refusing to update"
  and "destroy work nobody has a copy of". It surfaces rather than being swallowed:
  `isDeploySkew(state) === true` and the report callback received exactly `['diverged']`
  (leaf C2 subscribes to that callback; nothing here writes to the hub). Nothing moved —
  `git rev-parse HEAD` still equals the local head and `local-only.txt` still exists — and two
  further ticks do not wear the refusal down into a reset. The trigger itself is belt-and-
  braces: `deployUpdate` throws "refusing to update a checkout that is <kind>" for all five
  non-`behind` states. `deployInit`'s re-init path is fast-forward-only too (asserted:
  `git fetch origin main` + `git merge --ff-only origin/main`, no `reset`, no `--force`).

- [x] G4: agent typecheck exits 0
  CHECK: cd /home/bewinxed/whiffle && bun run --filter '@whiffle/agent' typecheck >/dev/null 2>&1 && echo CLEAN
  EXPECT: CLEAN
  EVIDENCE: `CLEAN`. `@whiffle/cli typecheck: Exited with code 0` as well. eslint clean on all
  seven touched files (deploy.ts, deploy.test.ts, update.ts, agent/index.ts, service.ts,
  cli.ts, deploy-init.test.ts) — no output.
