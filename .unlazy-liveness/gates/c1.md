# Gates: C1 — the deployment clone and the poller

Scope: packages/agent/src/deploy.ts (new), packages/agent/src/update.ts (trigger
integration), packages/cli/src/service.ts + a `cockpit deploy init` verb (service.ts is
yours now — B2 and D4 have landed). Contract C8: clone at ~/.cockpit/app tracking
origin/main, `.cockpit-deploy` marker, 60 s fetch-and-compare poller that ONLY acts in a
marked checkout, update flow unchanged (pull --ff-only, install, build, restart). A dev
tree must be structurally unable to auto-pull.

- [ ] G1: deploy init builds a working clone layout in a scratch dir (test uses a local bare repo as origin: clone, marker, units generated pointing at the clone with the C9 DB path)
  CHECK: cd /home/bewinxed/cockpit && bun test packages/cli/ packages/agent/ 2>&1 | grep -E "[0-9]+ fail" | tail -2
  EXPECT: 0 fail
  EVIDENCE: pending

- [ ] G2: poller acts only behind the marker — unmarked checkout: fetch/compare never runs (tested); marked + behind → update flow invoked exactly once per new head (tested with the scratch origin advancing)
  EVIDENCE: pending

- [ ] G3: the poller's pull is --ff-only against pinned origin/main; a diverged clone refuses loudly and surfaces on the board rather than resetting
  EVIDENCE: pending (quote the refusal path test)

- [ ] G4: agent typecheck exits 0
  CHECK: cd /home/bewinxed/cockpit && bun run --filter '@cockpit/agent' typecheck >/dev/null 2>&1 && echo CLEAN
  EXPECT: CLEAN
  EVIDENCE: pending
