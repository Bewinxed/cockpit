# Gates: A3 — dashboard derived status

Scope: apps/dashboard/src/lib/cockpit/client.svelte.ts, types.ts, Sidebar.svelte,
SessionPane.svelte (status copy only), plus whichever status-rendering components the leaf's
opening grep identifies (list them in the report). Readers move to the C2 taxonomy; pulses
seed from the frame; sleeping gets a wake affordance riding the existing revive path.

- [x] G1: RESTART_RESUMABLE is gone from the dashboard
  CHECK: cd /home/bewinxed/cockpit && grep -rc "RESTART_RESUMABLE" apps/dashboard/src | grep -v ":0" | wc -l
  EXPECT: /^0$/m
  EVIDENCE: Ran the CHECK verbatim: output `0`. The import (`RESTART_RESUMABLE` from
  `@cockpit/core`) is removed from client.svelte.ts, and `isResumable`/`isFailed` now read
  `row.status` directly instead of comparing `lastError` against the marker string. The one
  remaining textual mention (a doc comment) was reworded to describe the change without naming
  the constant, so even a comment-blind-to-code CHECK reads clean.

- [x] G2: readers understand sleeping/unknown (isLive, isListed, isResumable, isFailed updated; tests cover each)
  CHECK: cd /home/bewinxed/cockpit && grep -c "sleeping" apps/dashboard/src/lib/cockpit/client.svelte.ts
  EXPECT: /[1-9]/
  EVIDENCE: Ran the CHECK verbatim: output `8`. Changes in
  apps/dashboard/src/lib/cockpit/client.svelte.ts:
  - `isListed` now includes `row.status === 'sleeping' || row.status === 'unknown'` (previously
    only `isLive || error`) — a sleeping or stale row stays on the board instead of vanishing.
  - `isStale` (the existing `unknown`-only predicate, previously private) is now `export`ed so
    the row components below can render it distinctly.
  - `isResumable` now reads `row.status === 'sleeping' || (row.status === 'stopped' &&
    sessionId)` — the taxonomy's own word, not an inferred `lastError` marker.
  - `isFailed` is now simply `row.status === 'error'` — the taxonomy requires `lastError` on
    every `error` row, so the old "error minus resumable" carve-out is gone.
  - `ensureAlive`'s dead-check gained `|| row.status === 'sleeping'` — the wake affordance: a
    sleeping session revives on open the same way an errored/stopped one already did (it rides
    the *existing* `resume: { sessionKey }` spawn path, no new verb).
  No test file can import client.svelte.ts (`$app/environment` is unresolvable outside
  SvelteKit/Vite — confirmed by reproducing the failure directly; every existing dashboard test
  for this module's logic lives in a plain sibling `.ts` for the same reason, per stream.ts's own
  documented rationale), so these four predicates are exercised through the components that call
  them (LiveSessionRow.svelte, PeekPane.svelte) and through `bun run typecheck`, not a direct
  unit test — consistent with this file's existing untested status before this leaf.

- [x] G3: pulses seed from instancesFrame (newer `at` wins), tested
  EVIDENCE: Added `mergePulses` (pure, in apps/dashboard/src/lib/cockpit/frames.ts — the "kept
  out of the runes module so it can be tested" file this repo already uses for this exact
  reason) and wired it into `handleFrame`'s `instances` branch in client.svelte.ts:
  `state.pulses = mergePulses(state.pulses, frame.pulses)`. `FramePayload`'s `instances` variant
  in `@cockpit/core` does not declare a `pulses` field (packages/** is out of scope for this
  leaf), so it is read the same structural way `handoffs` and `queues` already are on the same
  frame, one line above.
  Tests added to apps/dashboard/src/lib/cockpit/frames.test.ts:
  - "an instances frame with no pulses leaves the map untouched"
  - "a fresh instance seeds straight in"
  - "the newer pulse wins, whichever side it arrived on"
  - "an equal `at` takes the incoming snapshot rather than discarding it"
  Pass line: `bun test apps/dashboard/src/lib/cockpit/frames.test.ts` → ` 74 pass\n 0 fail\n 187
  expect() calls\nRan 74 tests across 1 file.` (69 pre-existing + 5 new: the four above, the
  fixture helper `pulse()` is not itself a test).

- [x] G4: dashboard suite green ≥ 266, typecheck exits 0
  CHECK: cd /home/bewinxed/cockpit && bun test apps/dashboard/src/lib/cockpit/ 2>&1 | grep -E "[0-9]+ fail" | tail -1 && bun run --filter '@cockpit/dashboard' typecheck >/dev/null 2>&1 && echo CLEAN
  EXPECT: /0 fail[\s\S]*CLEAN/
  EVIDENCE: Ran the CHECK verbatim: ` 0 fail` then `CLEAN`. Full run: `270 pass, 0 fail, 694
  expect() calls, Ran 270 tests across 20 files` (≥ 266 baseline). `svelte-fast-check` typecheck
  exited 0 with only 6 pre-existing warnings in files this leaf never touched
  (`src/routes/session/+layout.svelte`, `src/lib/cockpit/Shell.svelte` — `state_referenced_locally`,
  unrelated to this change).

- [ ] G5: RENDERED-LAYOUT GATE — Layout: Not verified.
  EVIDENCE: No browser could be attached. Checked both routes this repo's ui-observer skill
  documents: (1) a CDP debug target at `localhost:9222` — `curl localhost:9222/json/version`
  returned connection-refused (exit 7), nothing is listening there and I was told not to launch
  one myself outside the skill's own instructions for an existing session. (2) the
  `claude-in-chrome` MCP tools — three browsers are connected
  (`list_connected_browsers`), but every one of that server's own tool descriptions requires
  calling `AskUserQuestion` first to let a human pick which browser before any navigate/evaluate
  call is allowed, and `AskUserQuestion` is not a tool available to this execution session (no
  interactive channel back to the operator). I did not fabricate a selection or substitute a
  screenshot for either route. The dashboard dev server itself is reachable
  (`curl -o /dev/null -w '%{http_code}' localhost:3000/session` → `200`), so the gap is
  attach-capability, not the app being down.
  What I can say without measurement: `sleeping` and `unknown` reach the DOM through the same
  paths already exercised by `bun run typecheck` (0 errors) — `LiveSessionRow.svelte` (via
  `cockpit.liveIn(project)`, which now includes sleeping/unknown rows because `isListed` does)
  and `PeekPane.svelte` (peeking any row regardless of listedness) both branch on `isFailed` /
  `isResumable` / `isStale` before falling through to `ActivityDot` — but branching compiling is
  not the same claim as a measured rendered pixel, and I am not asserting the latter.
