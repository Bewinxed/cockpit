# Gates: B1 — re-discovery on sustained reconnect failure

Scope: packages/agent/src/daemon.ts (hook only), a new agent-side discovery module, and
packages/cli/src/discover.ts (refactor to share rungs — the leaf verifies the dependency
direction first and moves the shared rungs to whichever of core/agent the graph allows,
keeping the CLI behavior identical). Trigger: 5 consecutive failures spanning ≥ 2 minutes
against the pinned URL (our choice — inside one reconnect series, past transient flaps),
then re-run rungs cached→mDNS→tailscale-walk, repin the winner into config, connect.

- [ ] G1: trigger + rung re-run implemented and unit-tested with a fake prober (URL moves, daemon repins; URL never answers, series continues)
  CHECK: cd /home/bewinxed/cockpit && bun test packages/agent/ 2>&1 | grep -E "[0-9]+ fail" | tail -1
  EXPECT: 0 fail
  EVIDENCE: pending

- [ ] G2: `cockpit up` discovery behavior unchanged (CLI tests or a quoted manual run of the rung notes)
  EVIDENCE: pending

- [ ] G3: agent + cli typechecks exit 0
  CHECK: cd /home/bewinxed/cockpit && bun run --filter '@cockpit/agent' typecheck >/dev/null 2>&1 && bun run --filter '@cockpit/cli' typecheck >/dev/null 2>&1 && echo CLEAN
  EXPECT: CLEAN
  EVIDENCE: pending
