# Gates: B1 — re-discovery on sustained reconnect failure

Scope: packages/agent/src/daemon.ts (hook only), a new agent-side discovery module, and
packages/cli/src/discover.ts (refactor to share rungs — the leaf verifies the dependency
direction first and moves the shared rungs to whichever of core/agent the graph allows,
keeping the CLI behavior identical). Trigger: 5 consecutive failures spanning ≥ 2 minutes
against the pinned URL (our choice — inside one reconnect series, past transient flaps),
then re-run rungs cached→mDNS→tailscale-walk, repin the winner into config, connect.

- [x] G1: trigger + rung re-run implemented and unit-tested with a fake prober (URL moves, daemon repins; URL never answers, series continues)
  CHECK: cd /home/bewinxed/cockpit && bun test packages/agent/ 2>&1 | grep -E "[0-9]+ fail" | tail -1
  EXPECT: 0 fail
  EVIDENCE: 0 fail

- [x] G2: `cockpit up` discovery behavior unchanged (CLI tests or a quoted manual run of the rung notes)
  EVIDENCE: `discoverHub()` in `packages/cli/src/discover.ts` now imports its mDNS/tailscale/
    probe rungs from `@cockpit/agent`'s new `discovery.ts` (moved there per the
    plan's "moves the shared rungs to whichever of core/agent the graph allows" —
    agent, because `@cockpit/cli` already depends on `@cockpit/agent`, so the
    reverse would cycle) but keeps its own `[n/5]` ladder, narration text, and
    flag/env/cached-config/localhost rungs verbatim. Ran it for real with a
    scratch `XDG_CONFIG_HOME` (never the live `~/.config/cockpit/config.json`)
    against the actual LAN:
      XDG_CONFIG_HOME=<scratch> COCKPIT_HUB_PORT=39999 bun <script calling discoverHub>
      → "[1/5] --hub / COCKPIT_HUB_URL: not set"
        "[2/5] no cached hub at <scratch>/cockpit/config.json"
        "[3/5] mDNS _cockpit._tcp.local: browsing 2000ms"
        "[3/5] mDNS advertised http://192.168.3.100:3456, http://10.89.0.1:3456: probing"
        "[3/5] http://192.168.3.100:3456 answered"
        RESULT: {"httpUrl":"http://192.168.3.100:3456","wsUrl":"ws://192.168.3.100:3456/ws","source":"mdns"}
    Identical rung order and identical narration format to the pre-refactor file
    (diffed by hand: only the mDNS/tailscale/probe bodies moved out, none of the
    log lines or the ladder's control flow changed). The scratch config file
    picked up the write; `stat` on the real `~/.config/cockpit/config.json`
    shows its mtime unchanged (12:12, before this 13:44 run) — the live config
    was never touched.

- [x] G3: agent + cli typechecks exit 0
  CHECK: cd /home/bewinxed/cockpit && bun run --filter '@cockpit/agent' typecheck >/dev/null 2>&1 && bun run --filter '@cockpit/cli' typecheck >/dev/null 2>&1 && echo CLEAN
  EXPECT: CLEAN
  EVIDENCE: CLEAN
