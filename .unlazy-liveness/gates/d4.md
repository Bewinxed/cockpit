# Gates: D4 — service management for sessiond

Scope: packages/cli/src/service.ts only. `cockpit-sessiond` joins SERVICES (Restart=on-failure,
RestartSec=2, StartLimitIntervalSec=0, default KillMode kept — design §11); agent unit gains
Wants=/After= sessiond; restart verb honors --when-idle/--force via restartDecision; the
ad-hoc pid-ledger orphan sweep (pid + start-ticks match) lands beside it. NO live restarts
in this leaf.

- [x] G1: sessiond spec present for both platforms via specFor, snapshot-tested
  CHECK: cd /home/bewinxed/cockpit && grep -c "sessiond" packages/cli/src/service.ts
  EXPECT: /[3-9]/
  EVIDENCE: 46

- [x] G2: agent unit orders after sessiond (Wants/After on linux, equivalent on darwin), quoted from the generated spec in tests
  CHECK: cd /home/bewinxed/cockpit && bun test packages/cli/src/service.test.ts -t "orders after sessiond" 2>&1 | grep -E "[0-9]+ (pass|fail)" | tr '\n' ' '
  EXPECT: /3 pass/
  EVIDENCE: `3 pass  0 fail` — packages/cli/src/service.test.ts, describe 'the agent orders after sessiond'.
    systemd (quoted from serviceDefinition('agent','prod','systemd')): the ordering lines are
    exactly ['Wants=cockpit-hub.service', 'Wants=cockpit-sessiond.service',
    'After=cockpit-hub.service', 'After=cockpit-sessiond.service'].
    launchd (quoted from serviceDefinition('agent','prod','launchd')): the plist contains
    `<!-- ordering: starts after dev.cockpit.hub, dev.cockpit.sessiond — launchd has no
    ordering, so this is recorded, not enforced -->`; launchd has no ordering primitive, so
    the equivalent is the ordering written into the artefact (a third test asserts the hub
    and sessiond plists get no such comment, so it is not blanket text).

- [x] G3: cli tests + typecheck green
  CHECK: cd /home/bewinxed/cockpit && bun test packages/cli/ 2>&1 | grep -E "[0-9]+ fail" | tail -1; bun run --filter '@cockpit/cli' typecheck >/dev/null 2>&1 && echo CLEAN
  EXPECT: /0 fail|CLEAN/
  EVIDENCE: 0 fail | CLEAN
