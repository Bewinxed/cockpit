# Gates: D5 — opencode server under sessiond

Scope: packages/agent/src/harnesses/opencode.ts (+ its tests). Design §4.2: `opencode serve`
as a sessiond spec-child, announced port read agent-side from the ring,
`createOpencodeClient` attach, SSE re-subscribe on supervisor rebuild; no event ring for
opencode (the server is its own event authority). If the opencode SDK surface has moved
since the design read (1.18.18), ABANDON with the observed surface quoted rather than
forcing it.

- [ ] G1: server spawn goes through sessiond; port parsing is agent-side (tested with a fake server child)
  CHECK: cd /home/bewinxed/cockpit && grep -c "createOpencodeClient" packages/agent/src/harnesses/opencode.ts
  EXPECT: /[1-9]/
  EVIDENCE: pending

- [ ] G2: supervisor teardown + rebuild re-attaches to the still-running server and re-subscribes SSE (tested)
  EVIDENCE: pending

- [ ] G3: agent suite green
  CHECK: cd /home/bewinxed/cockpit && bun test packages/agent/ 2>&1 | grep -E "[0-9]+ fail" | tail -1
  EXPECT: 0 fail
  EVIDENCE: pending
