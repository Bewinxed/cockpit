# Gates: D5 — opencode server under sessiond

Scope: packages/agent/src/harnesses/opencode.ts (+ its tests). Design §4.2: `opencode serve`
as a sessiond spec-child, announced port read agent-side from the ring,
`createOpencodeClient` attach, SSE re-subscribe on supervisor rebuild; no event ring for
opencode (the server is its own event authority). If the opencode SDK surface has moved
since the design read (1.18.18), ABANDON with the observed surface quoted rather than
forcing it.

SDK surface re-checked before building, NOT moved: `@opencode-ai/sdk` 1.18.18 in
packages/agent/node_modules still exports `createOpencodeClient(config?: Config & {directory?: string}): OpencodeClient`
(dist/client.d.ts:5) and `Config.baseUrl` (dist/gen/client/types.gen.d.ts:10).
Port announcement verified LIVE against the installed binary (opencode 1.18.19),
`opencode serve --hostname=127.0.0.1 --port=0` stdout:
    Warning: OPENCODE_SERVER_PASSWORD is not set; server is unsecured.
    opencode server listening on http://127.0.0.1:43663
— parseable, and parsed by the SDK's own rule (startsWith "opencode server listening",
then /on\s+(https?:\/\/[^\s]+)/). No abandon.

- [x] G1: server spawn goes through sessiond; port parsing is agent-side (tested with a fake server child)
  CHECK: cd /home/bewinxed/whiffle && grep -c "createOpencodeClient" packages/agent/src/harnesses/opencode.ts
  EXPECT: /[1-9]/
  EVIDENCE: grep -c "createOpencodeClient" packages/agent/src/harnesses/opencode.ts => 2 (import +
    `createOpencodeClient({ baseUrl: url })` in `#ensure`; `createOpencode` is gone).
    `attachOpencodeServer` spawns `opencode serve --hostname=127.0.0.1 --port=0` as a sessiond
    spec-child under procId `opencode-server`, then reads the port from the ring
    (`subscribe(procId, …, 0)`) via `parseServerAnnouncement` — sessiond parses nothing.
    Test (real sessiond subprocess on a scratch socket, scripted fake server child):
    `bun test packages/agent/src/harnesses/opencode-sessiond.test.ts` => 3 pass / 0 fail,
    19 expect() calls. It asserts the returned url matches /^http:\/\/127\.0\.0\.1:\d+$/,
    sessiond holds the child (`alive: true`, pid != process.pid), `GET {url}/subscriptions`
    answers 200, and a second attach returns the SAME url with exactly ONE held proc at the
    SAME pid (no rival server).

- [x] G2: supervisor teardown + rebuild re-attaches to the still-running server and re-subscribes SSE (tested)
  EVIDENCE: test 'a torn-down agent side rebuilds onto the still-running server and re-subscribes
    SSE' in packages/agent/src/harnesses/opencode-sessiond.test.ts. First life: attach, real
    `createOpencodeClient({baseUrl})`, SSE frame received, server reports {subscriptions: 1}.
    Then the agent side dies (`first.close()` + 250ms absence). Second life: new SessiondClient,
    `attachOpencodeServer` returns the SAME url, proc still `alive: true` at the SAME pid — no
    restart — and a fresh `createOpencodeClient` re-subscribes: server reports subscriptions >= 2.
    Adapter side matches: `dispose()` no longer closes the server (it closes only the sessiond
    socket), and `#ensure()` clears `#disposed` so the per-directory pumps re-subscribe.
    Run: 3 pass / 0 fail. No stray processes after: `ps -eo args | grep -E "sessiond/src/main.ts|fake-opencode-serve"` => empty.

- [x] G3: agent suite green
  CHECK: cd /home/bewinxed/whiffle && bun test packages/agent/ 2>&1 | grep -E "[0-9]+ fail" | tail -1
  EXPECT: 0 fail
  EVIDENCE: `bun test packages/agent/` => "158 pass / 0 fail / 500 expect() calls, Ran 158 tests
    across 21 files" (baseline 144, +3 mine and +11 from sibling leaves landing in the tree).
    Whole repo `bun test` => "612 pass / 0 fail / 1673 expect() calls, Ran 612 tests across 57
    files" (baseline 609, +3). `npx tsc --noEmit -p packages/agent` reports nothing in
    opencode.ts (only a pre-existing session.ts error owned by leaf D3).
    `npx eslint` on both owned files: the 3 findings in opencode.ts are byte-identical to the
    findings on the unmodified file (verified by stashing the change); the new test file is clean.
