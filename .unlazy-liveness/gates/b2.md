# Gates: B2 — the DB leaves the repo tree

Scope: packages/cli/src/service.ts (default WHIFFLE_DB_PATH → platform data dir) and
packages/hub/src/index.ts (boot migration: target absent + legacy present → move db/wal/shm).
`git clean` in the checkout must never again be able to delete the fleet's memory.

- [x] G1: no service spec points WHIFFLE_DB_PATH into a git checkout
  CHECK: cd /home/bewinxed/whiffle && grep -n "WHIFFLE_DB_PATH" packages/cli/src/service.ts
  EXPECT: /share\/whiffle|Application Support/
  EVIDENCE: 233:      WHIFFLE_DB_PATH: DEFAULT_DB_PATH, // ~/.local/share/whiffle or ~/Library/Application Support/whiffle

- [x] G2: boot migration moves an existing legacy file exactly once (test with scratch dirs: legacy present + target absent → moved with wal/shm; second boot → no-op; target already present → legacy untouched)
  CHECK: cd /home/bewinxed/whiffle && bun test packages/hub/ 2>&1 | grep -E "[0-9]+ fail" | tail -1
  EXPECT: 0 fail
  EVIDENCE: 0 fail

- [x] G3: hub + cli typechecks exit 0
  CHECK: cd /home/bewinxed/whiffle && bun run --filter '@whiffle/hub' typecheck >/dev/null 2>&1 && bun run --filter '@whiffle/cli' typecheck >/dev/null 2>&1 && echo CLEAN
  EXPECT: CLEAN
  EVIDENCE: CLEAN
